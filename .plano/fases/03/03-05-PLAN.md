---
phase: 03-frontend
plan: 03-05
type: feature
autonomous: true
wave: 3
depends_on: [03-01, 03-06]
requirements: [UI-02]
must_haves:
  truths:
    - "Expandir um participante no grid mostra a lista de faixas dele"
    - "Da para adicionar faixa numerada (inicio/fim), adicionar lote sem numero (quantidade), editar e remover cada faixa"
    - "Lote sem numero aparece como 'Sem numero (N cartoes)'"
    - "Apos mutar faixas, Recebidos do participante recalcula (vem do backend)"
  artifacts:
    - path: "frontend/src/components/eventos/FaixasPanel.tsx"
      provides: "Painel de CRUD de faixas do participante consumindo os endpoints de faixas (Fase 2)"
    - path: "frontend/src/app/(app)/eventos/[id]/page.tsx"
      provides: "renderExpanded do grid passa a renderizar FaixasPanel + historico"
  key_links:
    - from: "FaixasPanel"
      to: "POST/PUT/DELETE /eventos/{id}/participantes/{pid}/faixas[/{faixa_id}]"
      via: "api.post/put/delete; on success refetch do participante (sobe Recebidos)"
---

# Fase 03 Plano 05: Sub-linha expansivel de faixas

**Objetivo:** Na sub-linha expandida de cada participante (slot `renderExpanded` criado no 03-04), adicionar um painel de CRUD de faixas: listar faixas, adicionar faixa numerada (inicio/fim), adicionar lote sem numero (quantidade), editar e remover. Lote sem numero exibido como "Sem numero (N cartoes)". Cobre UI-02. Depende do grid (03-04) e dos tipos (03-01). Apos qualquer mutacao, o participante e refeito (Recebidos derivado sobe).

## Contexto

@frontend/src/app/(app)/eventos/[id]/page.tsx — `renderExpanded` foi adicionado ao grid no plano 03-04 e populado com o historico no plano 03-06. `refetchParticipante` (do 03-04) sincroniza 1 participante. `pagamentos`, `contaNome`, `handleEstornar` continuam disponiveis para o historico.
@frontend/src/types/index.ts — `FaixaOut`, `FaixaCreate`, `FaixaUpdate`, `ParticipanteOut.faixas` (plano 03-01).
@frontend/src/lib/api.ts — `api.post/put/delete`. Erro -> `Error(data.detail)`.
@frontend/src/components/ui/index.ts — `Button`, `Input`.
@.plano/SYSTEM-DESIGN.md — secao 5.1/5.2: rotas de faixas. Regras (5.2): numerada exige inicio+fim, fim>=ini, quantidade derivada no servidor; sem_numero exige quantidade>=1. DELETE que deixaria recebidos < vendidos+devolvidos+pagou_custo retorna 400.

Endpoints (Fase 2, todos sob `/api/eventos`):
- `POST /{evento_id}/participantes/{pid}/faixas` body `FaixaCreate` -> retorna `ParticipanteOut`
- `PUT /{evento_id}/participantes/{pid}/faixas/{faixa_id}` body `FaixaUpdate` -> `ParticipanteOut`
- `DELETE /{evento_id}/participantes/{pid}/faixas/{faixa_id}` -> `ParticipanteOut`

Nota: os endpoints retornam `ParticipanteOut` (SYSTEM-DESIGN 5.2 "retornar ParticipanteOut"), entao apos cada mutacao da pra atualizar o participante direto com a resposta (sem refetch extra). Usar a resposta quando disponivel; cair em `refetchParticipante` se a resposta nao vier tipada.

Convencoes: UI kit, sem nova identidade. Toast PT-BR sem em-dash. snake_case nos payloads.

## Tarefas

<task id="1" type="auto">
<files>frontend/src/components/eventos/FaixasPanel.tsx</files>
<action>
Criar o painel de faixas. Caminho novo: `frontend/src/components/eventos/FaixasPanel.tsx`.

```tsx
"use client";

import { useState } from "react";
import { Plus, Pencil, Trash2, Check, X } from "lucide-react";
import toast from "react-hot-toast";
import { api } from "@/lib/api";
import { Button, Input } from "@/components/ui";
import type { ParticipanteOut, FaixaOut } from "@/types";

interface FaixasPanelProps {
  eventoId: string;
  participante: ParticipanteOut;
  /** Chamado apos qualquer mutacao para o pai sincronizar o participante. */
  onMutated: (atualizado?: ParticipanteOut) => void;
}

export function FaixasPanel({ eventoId, participante, onMutated }: FaixasPanelProps) {
  const base = `/eventos/${eventoId}/participantes/${participante.id}/faixas`;
  const [addNum, setAddNum] = useState({ inicio: "", fim: "" });
  const [addLote, setAddLote] = useState("");
  const [editId, setEditId] = useState<number | null>(null);
  const [editForm, setEditForm] = useState({ inicio: "", fim: "", quantidade: "" });
  const [busy, setBusy] = useState(false);

  const after = (resp: ParticipanteOut | unknown) => {
    onMutated(resp && typeof resp === "object" && "id" in (resp as object) ? (resp as ParticipanteOut) : undefined);
  };

  const handleAddNumerada = async () => {
    const ini = parseInt(addNum.inicio, 10);
    const fim = parseInt(addNum.fim, 10);
    if (isNaN(ini) || isNaN(fim) || fim < ini) {
      toast.error("Informe inicio e fim validos (fim >= inicio)");
      return;
    }
    try {
      setBusy(true);
      const resp = await api.post<ParticipanteOut>(base, { sem_numero: false, numero_inicio: ini, numero_fim: fim });
      toast.success("Faixa adicionada");
      setAddNum({ inicio: "", fim: "" });
      after(resp);
    } catch (err: unknown) {
      toast.error(err instanceof Error ? err.message : "Erro ao adicionar faixa");
    } finally { setBusy(false); }
  };

  const handleAddLote = async () => {
    const qtd = parseInt(addLote, 10);
    if (isNaN(qtd) || qtd < 1) {
      toast.error("Informe a quantidade (>= 1)");
      return;
    }
    try {
      setBusy(true);
      const resp = await api.post<ParticipanteOut>(base, { sem_numero: true, quantidade: qtd });
      toast.success("Lote adicionado");
      setAddLote("");
      after(resp);
    } catch (err: unknown) {
      toast.error(err instanceof Error ? err.message : "Erro ao adicionar lote");
    } finally { setBusy(false); }
  };

  const startEdit = (f: FaixaOut) => {
    setEditId(f.id);
    setEditForm({
      inicio: f.numero_inicio != null ? String(f.numero_inicio) : "",
      fim: f.numero_fim != null ? String(f.numero_fim) : "",
      quantidade: String(f.quantidade),
    });
  };

  const saveEdit = async (f: FaixaOut) => {
    try {
      setBusy(true);
      const body = f.sem_numero
        ? { sem_numero: true, quantidade: parseInt(editForm.quantidade, 10) || 0 }
        : { sem_numero: false, numero_inicio: parseInt(editForm.inicio, 10), numero_fim: parseInt(editForm.fim, 10) };
      const resp = await api.put<ParticipanteOut>(`${base}/${f.id}`, body);
      toast.success("Faixa atualizada");
      setEditId(null);
      after(resp);
    } catch (err: unknown) {
      toast.error(err instanceof Error ? err.message : "Erro ao editar faixa");
    } finally { setBusy(false); }
  };

  const remove = async (f: FaixaOut) => {
    if (!confirm("Remover esta faixa?")) return;
    try {
      setBusy(true);
      const resp = await api.delete<ParticipanteOut>(`${base}/${f.id}`);
      toast.success("Faixa removida");
      after(resp);
    } catch (err: unknown) {
      toast.error(err instanceof Error ? err.message : "Erro ao remover faixa");
    } finally { setBusy(false); }
  };

  const label = (f: FaixaOut) =>
    f.sem_numero
      ? `Sem numero (${f.quantidade} cartoes)`
      : `${f.numero_inicio} - ${f.numero_fim} (${f.quantidade} cartoes)`;

  return (
    <div className="space-y-2">
      <p className="text-xs text-txt-tertiary uppercase tracking-wider font-body">Faixas de cartao</p>

      {participante.faixas.length === 0 ? (
        <p className="text-xs text-txt-tertiary font-body">Nenhuma faixa cadastrada.</p>
      ) : (
        <ul className="space-y-1">
          {participante.faixas.map((f) => (
            <li key={f.id} className="flex items-center gap-2 text-xs">
              {editId === f.id ? (
                <>
                  {f.sem_numero ? (
                    <Input label="" type="number" min={1} value={editForm.quantidade}
                      onChange={(e) => setEditForm((p) => ({ ...p, quantidade: e.target.value }))}
                      containerClassName="w-24" />
                  ) : (
                    <>
                      <Input label="" type="number" value={editForm.inicio}
                        onChange={(e) => setEditForm((p) => ({ ...p, inicio: e.target.value }))}
                        containerClassName="w-20" placeholder="ini" />
                      <Input label="" type="number" value={editForm.fim}
                        onChange={(e) => setEditForm((p) => ({ ...p, fim: e.target.value }))}
                        containerClassName="w-20" placeholder="fim" />
                    </>
                  )}
                  <Button size="sm" icon={<Check size={13} />} loading={busy} onClick={() => saveEdit(f)}>Salvar</Button>
                  <button onClick={() => setEditId(null)} className="text-txt-tertiary hover:text-txt-primary"><X size={14} /></button>
                </>
              ) : (
                <>
                  <span className="font-mono text-txt-secondary">{label(f)}</span>
                  <div className="flex-1" />
                  <button onClick={() => startEdit(f)} className="text-txt-tertiary hover:text-brand-red" title="Editar"><Pencil size={13} /></button>
                  <button onClick={() => remove(f)} className="text-txt-tertiary hover:text-red-400" title="Remover"><Trash2 size={13} /></button>
                </>
              )}
            </li>
          ))}
        </ul>
      )}

      <div className="flex flex-wrap items-end gap-2 pt-1">
        <Input label="Inicio" type="number" containerClassName="w-24" value={addNum.inicio}
          onChange={(e) => setAddNum((p) => ({ ...p, inicio: e.target.value }))} />
        <Input label="Fim" type="number" containerClassName="w-24" value={addNum.fim}
          onChange={(e) => setAddNum((p) => ({ ...p, fim: e.target.value }))} />
        <Button size="sm" icon={<Plus size={13} />} loading={busy} onClick={handleAddNumerada}>Faixa numerada</Button>
        <div className="w-px h-8 bg-border-subtle mx-1" />
        <Input label="Qtd (sem numero)" type="number" containerClassName="w-32" value={addLote}
          onChange={(e) => setAddLote(e.target.value)} />
        <Button size="sm" variant="secondary" icon={<Plus size={13} />} loading={busy} onClick={handleAddLote}>Lote sem numero</Button>
      </div>
    </div>
  );
}
```

Comportamento: lista cada faixa com label apropriado (numerada `ini - fim (N cartoes)`, sem numero `Sem numero (N cartoes)`). Editar in-place (inicio/fim para numerada, quantidade para lote). Remover com confirm. Adicionar numerada (inicio/fim) e lote (quantidade). Cada mutacao chama `onMutated` com o `ParticipanteOut` retornado pelo endpoint (Recebidos ja recalculado no backend).
</action>
<verify><automated>test -f "/home/projects/Whatsapp-Velhos/frontend/src/components/eventos/FaixasPanel.tsx" && grep -nE "Sem numero \(\$\{f.quantidade\} cartoes\)|Faixa numerada|Lote sem numero|sem_numero: true" "/home/projects/Whatsapp-Velhos/frontend/src/components/eventos/FaixasPanel.tsx"</automated></verify>
<done>FaixasPanel existe: lista faixas, add numerada, add lote sem numero, editar, remover; lote exibido como "Sem numero (N cartoes)".</done>
</task>

<task id="2" type="auto">
<files>frontend/src/app/(app)/eventos/[id]/page.tsx</files>
<action>
Integrar o FaixasPanel ao slot expandido do grid. 

1. Importar no topo: `import { FaixasPanel } from "@/components/eventos/FaixasPanel";`
2. No `renderExpanded` passado ao `<ParticipantesGrid>` (criado no 03-04), inserir o FaixasPanel ANTES do bloco de historico. Substituir o corpo do `renderExpanded` por:

```tsx
renderExpanded={(p) => {
  const hist = pagamentos.filter((pg) => pg.evento_participante_id === p.id);
  return (
    <div className="space-y-3">
      <FaixasPanel
        eventoId={eventoId}
        participante={p}
        onMutated={(atualizado) => {
          if (atualizado) {
            setParticipantes((prev) => prev.map((x) => (x.id === atualizado.id ? atualizado : x)));
          } else {
            refetchParticipante(p.id);
          }
          api.get<EventoResumo>(`/eventos/${eventoId}/resumo`).then(setResumo).catch(() => {});
        }}
      />
      <div className="border-t border-border-subtle pt-2">
        {hist.length === 0 ? (
          <p className="text-xs text-txt-tertiary font-body py-1">Sem pagamentos registrados</p>
        ) : (
          <div className="space-y-1">
            <p className="text-xs text-txt-tertiary uppercase tracking-wider font-body mb-1">Historico</p>
            {hist.map((pg) => (
              <div key={pg.id} className="flex items-center gap-3 text-xs py-1 px-2 rounded hover:bg-surface-card">
                <span className="font-mono text-txt-secondary">{pg.data}</span>
                <span className="font-mono text-emerald-400">{formatCurrency(pg.valor)}</span>
                {pg.forma_pagto && <span className="text-txt-tertiary uppercase">{pg.forma_pagto}</span>}
                {contaNome(pg.conta_id) && <span className="text-txt-secondary">{contaNome(pg.conta_id)}</span>}
                <div className="flex-1" />
                <button onClick={() => handleEstornar(pg.id)} className="text-txt-tertiary hover:text-red-400" title="Estornar">
                  <RotateCcw size={12} />
                </button>
              </div>
            ))}
          </div>
        )}
      </div>
    </div>
  );
}}
```

`refetchParticipante`, `setParticipantes`, `setResumo` e `EventoResumo` ja existem/importados (planos anteriores). `onMutated` usa o participante retornado pelo endpoint quando disponivel, senao refaz; sempre reatualiza o resumo (Recebidos/valor podem mudar).
</action>
<verify><automated>grep -nE "<FaixasPanel|onMutated=\{" /home/projects/Whatsapp-Velhos/frontend/src/app/(app)/eventos/[id]/page.tsx</automated></verify>
<done>renderExpanded renderiza FaixasPanel + historico; onMutated atualiza participante e resumo.</done>
</task>

<task id="3" type="auto">
<files>frontend/src/app/(app)/eventos/[id]/page.tsx</files>
<action>
Build limpo. Rodar a partir de `frontend/`:

```bash
cd /home/projects/Whatsapp-Velhos/frontend && npx tsc --noEmit && npm run build 2>&1 | tail -25
```

Corrigir erros de tipo e remover imports orfaos que quebram o build (padrao do projeto). Com o grid editando cartoes inline (03-04) e faixas no painel (este plano), o modal "Cartoes" antigo e `openCartoesModal`/`handleSaveCartoes`/estados de `cartoesForm` podem ter ficado sem uso -> se o build apontar como nao usado, REMOVER o modal Cartoes (JSX ~L1143-1221), o handler, o opener e os estados relacionados (`cartoesModalOpen`, `cartoesParticipante`, `cartoesForm`, `cartoesSaving`). Esse e o momento certo de aposentar o modal pesado, pois o inline + faixas o substituem totalmente. Garantir que nenhuma outra parte referencia esses simbolos antes de remover.
</action>
<verify><automated>cd /home/projects/Whatsapp-Velhos/frontend && npx tsc --noEmit 2>&1 | grep -E "error TS" | head -5 || echo "tsc OK"</automated></verify>
<done>`npx tsc --noEmit` e `npm run build` passam; modal Cartoes antigo aposentado se ficou orfao; sem imports orfaos.</done>
</task>

<task id="4" type="checkpoint:human-verify">
<files>frontend/src/components/eventos/FaixasPanel.tsx</files>
<action>
Verificacao visual (UI-02). Backend Fase 2 rodando. Abrir `/eventos/[id]`, expandir um participante. Capturar e validar:
1. Painel "Faixas de cartao" listando as faixas existentes.
2. Adicionar faixa numerada quebrada (ex: 1-12, depois 45-50) -> ambas aparecem, Recebidos na linha do grid sobe para 12+6=18.
3. Adicionar lote sem numero (ex: 5) -> aparece "Sem numero (5 cartoes)", Recebidos sobe para 23.
4. Editar uma faixa (mudar fim) -> quantidade e Recebidos recalculam.
5. Remover uma faixa -> some e Recebidos cai. Se remover deixar Recebidos < vendidos+devolvidos+custo, backend retorna 400 e toast aparece (faixa permanece).

Anexar capturas (lista de faixas, add numerada quebrada, lote sem numero, Recebidos recalculado).
</action>
<verify><automated>FALTANDO — prova visual: capturas do painel de faixas com add numerada quebrada, lote sem numero "Sem numero (N cartoes)", edicao e remocao com Recebidos recalculando.</automated></verify>
<done>Capturas comprovam CRUD de faixas (numerada quebrada + lote sem numero), label correto do lote e Recebidos derivado atualizando.</done>
</task>

## Criterios de Sucesso

- [ ] Expandir participante mostra painel de faixas
- [ ] Adicionar faixa numerada (inicio/fim) funciona, inclusive faixas quebradas
- [ ] Adicionar lote sem numero (quantidade) funciona e exibe "Sem numero (N cartoes)"
- [ ] Editar e remover faixa funcionam; remocao invalida retorna 400 com toast
- [ ] Recebidos do participante recalcula apos mutacao de faixa
- [ ] `npx tsc --noEmit` e `npm run build` passam; modal antigo aposentado se orfao
- [ ] Capturas visuais anexadas (UI-02)
