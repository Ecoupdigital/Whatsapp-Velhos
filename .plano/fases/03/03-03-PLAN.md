---
phase: 03-frontend
plan: 03-03
type: feature
autonomous: true
wave: 1
depends_on: [03-01]
requirements: [UI-05, TEST-04]
must_haves:
  truths:
    - "A tela do evento mostra um card 'Relacao Cru x Assado' quando ha tipos definidos"
    - "Por tipo, o card mostra total vendido e total pedido lidos de resumo.itens_por_tipo"
    - "O card mostra o total a repassar a cozinha (vendido + pedido por tipo e somatorio geral)"
  artifacts:
    - path: "frontend/src/app/(app)/eventos/[id]/page.tsx"
      provides: "Card de estatistica consolidada cru x assado lendo resumo.itens_por_tipo"
  key_links:
    - from: "Card estatistica (page.tsx)"
      to: "GET /eventos/{id}/resumo"
      via: "resumo.itens_por_tipo ja carregado em fetchAll"
---

# Fase 03 Plano 03: Card de estatistica consolidada (cru x assado)

**Objetivo:** Adicionar um card de resumo "Relacao Cru x Assado" na tela do evento que le `resumo.itens_por_tipo` (vindo do `GET /resumo`, Fase 2) e mostra, por tipo, o total vendido e o total pedido, alem do total a repassar a cozinha (vendido + pedido). Cobre UI-05 e TEST-04. Risco baixo: e um card somente-leitura, independente do grid. Pode ser feito em paralelo com 03-02 (so depende dos tipos do 03-01).

## Contexto

@frontend/src/app/(app)/eventos/[id]/page.tsx — o card "Stats Cartoes" existente fica em ~L748-793 (bom modelo visual: usa `bg-surface-card border border-border-subtle rounded-lg p-4`, grid de numeros centralizado, `font-mono`). `resumo` (estado `EventoResumo | null`) ja e carregado em `fetchAll` (~L221) via `GET /eventos/${eventoId}/resumo`. `formatCurrency` importado de `@/lib/utils`.
@frontend/src/types/index.ts — `EventoResumo.itens_por_tipo: ResumoItemTipo[]` e `ResumoItemTipo { tipo, total_vendido, total_pedido }` (plano 03-01).

Convencoes: reusar estilo do card de cartoes (mesma identidade), `motion.div` com `initial/animate`, icone lucide. Idioma PT-BR sem em-dash. NAO calcular valor monetario do repasse aqui (o card trabalha com QUANTIDADES de itens cru/assado; "total a repassar a cozinha" e em unidades, nao R$). Se o briefing exigir R$, deixar so unidades por enquanto (decisao registrada no SYSTEM-DESIGN 5.3: "vendido + pedido por tipo e somatorio").

## Tarefas

<task id="1" type="auto">
<files>frontend/src/app/(app)/eventos/[id]/page.tsx</files>
<action>
Adicionar import do icone. Na lista de imports do `lucide-react` (~L6-28), adicionar `Flame` (representa cozinha/assado). Linha exemplo: `Flame,` junto aos outros.
</action>
<verify><automated>grep -nE "^\s*Flame," /home/projects/Whatsapp-Velhos/frontend/src/app/(app)/eventos/[id]/page.tsx</automated></verify>
<done>Icone Flame importado de lucide-react.</done>
</task>

<task id="2" type="auto">
<files>frontend/src/app/(app)/eventos/[id]/page.tsx</files>
<action>
Inserir o card de estatistica logo APOS o bloco "Stats Cartoes" (fecha em ~L793, antes do bloco "Toolbar + Filtros" em ~L795). Renderizar somente quando ha tipos e dados:

```tsx
{/* ── Relacao Cru x Assado ──────────────────────────────── */}
{resumo && resumo.itens_por_tipo && resumo.itens_por_tipo.length > 0 && (
  <motion.div
    initial={{ opacity: 0, y: 20 }}
    animate={{ opacity: 1, y: 0 }}
    transition={{ delay: 0.09 }}
    className="bg-surface-card border border-border-subtle rounded-lg p-4"
  >
    <div className="flex items-center gap-2 mb-3">
      <Flame size={16} className="text-orange-400" />
      <p className="text-xs font-display uppercase tracking-wider text-txt-secondary">
        Relacao Cru x Assado (a repassar)
      </p>
    </div>

    <div className="overflow-x-auto">
      <table className="w-full text-sm">
        <thead>
          <tr className="text-left text-txt-tertiary font-body text-xs uppercase tracking-wider">
            <th className="py-1.5 pr-4 font-medium">Tipo</th>
            <th className="py-1.5 px-4 font-medium text-right">Vendido</th>
            <th className="py-1.5 px-4 font-medium text-right">Pedido</th>
            <th className="py-1.5 pl-4 font-medium text-right">Total a repassar</th>
          </tr>
        </thead>
        <tbody>
          {resumo.itens_por_tipo.map((it) => (
            <tr key={it.tipo} className="border-t border-border-subtle">
              <td className="py-2 pr-4 capitalize text-txt-primary font-body">{it.tipo}</td>
              <td className="py-2 px-4 text-right font-mono text-emerald-400">{it.total_vendido}</td>
              <td className="py-2 px-4 text-right font-mono text-blue-400">{it.total_pedido}</td>
              <td className="py-2 pl-4 text-right font-mono font-bold text-txt-primary">
                {it.total_vendido + it.total_pedido}
              </td>
            </tr>
          ))}
        </tbody>
        <tfoot>
          <tr className="border-t-2 border-border">
            <td className="py-2 pr-4 text-txt-secondary font-display uppercase text-xs tracking-wider">Total geral</td>
            <td className="py-2 px-4 text-right font-mono text-emerald-400">
              {resumo.itens_por_tipo.reduce((s, it) => s + it.total_vendido, 0)}
            </td>
            <td className="py-2 px-4 text-right font-mono text-blue-400">
              {resumo.itens_por_tipo.reduce((s, it) => s + it.total_pedido, 0)}
            </td>
            <td className="py-2 pl-4 text-right font-mono font-bold text-orange-400">
              {resumo.itens_por_tipo.reduce((s, it) => s + it.total_vendido + it.total_pedido, 0)}
            </td>
          </tr>
        </tfoot>
      </table>
    </div>
  </motion.div>
)}
```

Logica do "total a repassar": por tipo = `total_vendido + total_pedido` (cartoes vendidos a clientes + o que o proprio jogador pediu para consumo). A linha de rodape soma todas as colunas. Card so aparece quando `itens_por_tipo` tem ao menos 1 entrada (evento sem split nao mostra).
</action>
<verify><automated>grep -nE "Relacao Cru x Assado|itens_por_tipo\.map|Total a repassar" /home/projects/Whatsapp-Velhos/frontend/src/app/(app)/eventos/[id]/page.tsx</automated></verify>
<done>Card renderiza tabela por tipo (vendido, pedido, total a repassar) + linha de total geral, condicionado a itens_por_tipo nao vazio.</done>
</task>

<task id="3" type="auto">
<files>frontend/src/app/(app)/eventos/[id]/page.tsx</files>
<action>
Garantir compilacao. Rodar a partir de `frontend/`:

```bash
cd /home/projects/Whatsapp-Velhos/frontend && npx tsc --noEmit 2>&1 | grep "eventos/\[id\]/page.tsx" || echo OK
```

Corrigir erros novos. Possivel ponto: `resumo.itens_por_tipo` pode ser `undefined` se o backend antigo nao retornar -> o guard `resumo.itens_por_tipo && resumo.itens_por_tipo.length > 0` ja protege em runtime; em TS, como o tipo do plano 03-01 declara `itens_por_tipo: ResumoItemTipo[]` (nao opcional), o acesso e seguro. Se preferir robustez contra backend desatualizado, o guard com `&&` ja basta.
</action>
<verify><automated>cd /home/projects/Whatsapp-Velhos/frontend && npx tsc --noEmit 2>&1 | grep "eventos/\[id\]/page.tsx" || echo OK</automated></verify>
<done>tsc sem erro novo em page.tsx.</done>
</task>

<task id="4" type="checkpoint:human-verify">
<files>frontend/src/app/(app)/eventos/[id]/page.tsx</files>
<action>
Verificacao visual (TEST-04). Com backend Fase 2 rodando e um evento Galeto que tenha tipos_item=["cru","assado"] e participantes com itens preenchidos (qtd_vendido/qtd_pedido por tipo), abrir `/eventos/[id]`. Capturar screenshot mostrando o card "Relacao Cru x Assado (a repassar)" com:
1. Uma linha por tipo (cru, assado) com Vendido, Pedido e Total a repassar.
2. A linha "Total geral" somando corretamente.

Validar manualmente que os numeros batem com a soma esperada dos participantes. Anexar a captura. Caso nenhum item esteja preenchido, o card nao aparece (comportamento correto) e a prova deve ser feita apos preencher via grid (plano 03-04) ou via API direta.
</action>
<verify><automated>FALTANDO — prova visual: captura do card Relacao Cru x Assado com totais por tipo e total geral.</automated></verify>
<done>Captura mostra a relacao consolidada cru/assado (vendido + pedido) e total a repassar batendo com os dados.</done>
</task>

## Criterios de Sucesso

- [ ] Card "Relacao Cru x Assado" aparece quando ha `itens_por_tipo`
- [ ] Por tipo: total vendido e total pedido exibidos
- [ ] Total a repassar = vendido + pedido por tipo e somatorio geral
- [ ] Le exclusivamente de `resumo.itens_por_tipo` (sem fetch extra)
- [ ] `npx tsc --noEmit` sem erro novo
- [ ] Captura visual anexada (TEST-04)
