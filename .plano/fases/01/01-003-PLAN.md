---
phase: 01-backend-api-publica
plan: 01-003
type: feature
autonomous: true
wave: 2
depends_on: ["01-001", "01-002"]
requirements: [API-01, API-02, API-03, API-04, API-05, API-07, SEC-01, SEC-02, SEC-03]
must_haves:
  truths:
    - "A suite test_portal.py prova que GET /api/portal responde 200 sem token"
    - "A suite prova o shape exato do payload (4 chaves de topo + sub-blocos)"
    - "A suite prova as regras de negocio: saldo, fluxo<=12, liquido de evento, filtro de eventos, atrasos COUNT"
    - "A suite prova a privacidade: varre o JSON e garante que nenhum nome de jogador aparece junto de dado de pagamento; atrasos sao int"
  artifacts:
    - path: "backend/tests/test_portal.py"
      provides: "Suite pytest completa do endpoint publico (funcional + privacidade)"
  key_links:
    - from: "tests/test_portal.py"
      to: "fixture seed_portal_data + client (conftest)"
      via: "TestClient sem header Authorization sobre app real"
    - from: "test de privacidade"
      to: "payload JSON serializado"
      via: "varredura recursiva de strings"
---

# Fase 01 Plano 003: Suite pytest do portal (funcional + trava de privacidade)

**Objetivo:** Preencher `backend/tests/test_portal.py` (scaffold criado no 01-001) com a suite completa que prova, de forma automatizada, todos os REQs da fase: 200 sem token, shape do payload, regras de caixa/eventos/jogos, e a TRAVA DE PRIVACIDADE (varredura do JSON garantindo que nenhum nome de jogador aparece em contexto financeiro e que atrasos sao int). Depende de 01-001 (schemas + fixture seed) e 01-002 (endpoint funcionando).

## Contexto

@backend/tests/conftest.py - Fixtures prontas: `client` (TestClient sobre a app real, com `get_db` e `get_current_user` overrides, scheduler neutralizado), `TestingSession`, `db_engine` (sqlite in-memory StaticPool). IMPORTANTE: o `client` faz override de `get_current_user`, mas o portal NAO depende dele, entao bater sem header funciona; para provar "sem token" basta nao mandar `Authorization` (o portal nao tem a dependencia).
@backend/tests/test_portal.py - Ja contem (do 01-001): docstring, helpers `_hoje`/`_mes_corrente`, fixture `seed_portal_data` (retorna dict de esperados), e `test_schemas_portal_importam_e_shape_topo`. ADICIONAR os testes abaixo NESTE arquivo, sem remover o que ja existe.
@backend/routers/portal.py - O endpoint sob teste (criado no 01-002).
@.plano/SYSTEM-DESIGN.md - Secao 6: a verificacao automatizavel exigida e "o payload nao deve conter nenhuma string de nome fora de jogos.*". Adversario e nome de time, nao PII (permitido em jogos.*).

## Tarefas

<task id="1" type="auto">
<files>backend/tests/test_portal.py</files>
<action>
Adicionar ao fim de `backend/tests/test_portal.py` os testes funcionais. Todos usam o `client` (conftest) e a fixture `seed_portal_data`. O `client` ja aplica `get_db` apontando pro mesmo `db_engine` que a `seed_portal_data` populou (ambos via `TestingSession` ligado ao mesmo `db_engine` por StaticPool). Bater no endpoint via `client.get("/api/portal")` SEM passar header `Authorization`.

```python
# --- API-01 / API-02: responde 200 sem token, com as 4 chaves de topo ---

def test_portal_responde_200_sem_token(client, seed_portal_data):
    r = client.get("/api/portal")  # sem header Authorization
    assert r.status_code == 200, r.text
    body = r.json()
    assert set(body.keys()) == {"meta", "caixa", "eventos", "jogos"}


def test_portal_meta(client, seed_portal_data):
    body = client.get("/api/portal").json()
    assert body["meta"]["time_nome"] == "Velhos Parceiros F.C."
    assert isinstance(body["meta"]["atualizado_em"], str) and body["meta"]["atualizado_em"]


# --- API-03: bloco caixa ---

def test_portal_caixa_saldo_e_totais(client, seed_portal_data):
    exp = seed_portal_data
    caixa = client.get("/api/portal").json()["caixa"]
    assert caixa["saldo_atual"] == exp["saldo_atual"]
    assert caixa["total_entrou"] == exp["total_entrou"]
    assert caixa["total_saiu"] == exp["total_saiu"]
    assert caixa["entrou_mes"] == exp["entrou_mes"]
    assert caixa["saiu_mes"] == exp["saiu_mes"]


def test_portal_fluxo_12m_max_12_itens(client, seed_portal_data):
    fluxo = client.get("/api/portal").json()["caixa"]["fluxo_12m"]
    assert len(fluxo) <= 12
    # ordem cronologica asc
    meses = [f["mes"] for f in fluxo]
    assert meses == sorted(meses)
    for f in fluxo:
        assert set(f.keys()) == {"mes", "entradas", "saidas"}


# --- SEC-01: atrasos sao COUNT inteiros ---

def test_portal_atrasos_count_int(client, seed_portal_data):
    exp = seed_portal_data
    atrasos = client.get("/api/portal").json()["caixa"]["atrasos"]
    assert isinstance(atrasos["mensalidades"], int)
    assert isinstance(atrasos["jogadores"], int)
    assert atrasos["mensalidades"] == exp["atrasos_mensalidades"]   # 2
    assert atrasos["jogadores"] == exp["atrasos_jogadores"]         # 2
    # garante que nao virou bool (bool e subclasse de int em python)
    assert not isinstance(atrasos["mensalidades"], bool)


# --- API-04 / API-07: eventos (liquido + filtro) ---

def test_portal_evento_liquido_e_custo_origem(client, seed_portal_data):
    exp = seed_portal_data
    eventos = client.get("/api/portal").json()["eventos"]
    titulos = {e["titulo"] for e in eventos}
    # cancelado e planejado-sem-arrecadacao nao aparecem
    assert "Baile Cancelado" not in titulos
    assert "Viagem Futura" not in titulos
    assert "Galeto Junho" in titulos
    galeto = next(e for e in eventos if e["titulo"] == "Galeto Junho")
    assert galeto["arrecadado"] == exp["evento_ok_arrecadado"]   # 400
    assert galeto["custo"] == exp["evento_ok_custo"]             # 200 (custo_real > 0)
    assert galeto["custo_origem"] == "real"
    assert galeto["liquido"] == exp["evento_ok_liquido"]         # 200
    assert galeto["liquido"] == galeto["arrecadado"] - galeto["custo"]


# --- API-05: jogos ---

def test_portal_jogos_resumo_e_rankings(client, seed_portal_data):
    jogos = client.get("/api/portal").json()["jogos"]
    resumo = jogos["resumo"]
    # seed: 1 vitoria (3x1), 1 derrota (0x2)
    assert resumo["vitorias"] == 1
    assert resumo["empates"] == 0
    assert resumo["derrotas"] == 1
    assert resumo["gols_pro"] == 3
    assert resumo["gols_contra"] == 3
    # artilharia: Carlao (2), Pedrinho (1)
    art = {e["nome"]: e["quantidade"] for e in jogos["artilharia"]}
    assert art.get("Carlao") == 2
    assert art.get("Pedrinho") == 1
    # ranking ordenado desc
    qts = [e["quantidade"] for e in jogos["artilharia"]]
    assert qts == sorted(qts, reverse=True)


def test_portal_resultados_e_proximos(client, seed_portal_data):
    jogos = client.get("/api/portal").json()["jogos"]
    # ultimos_resultados: realizados, recentes primeiro, placar "GFxGC"
    placares = {r["adversario"]: r["placar"] for r in jogos["ultimos_resultados"]}
    assert placares["Time A"] == "3x1"
    assert placares["Time B"] == "0x2"
    datas = [r["data"] for r in jogos["ultimos_resultados"]]
    assert datas == sorted(datas, reverse=True)
    # proximos_jogos: futuro (2099) presente, com horario/local
    prox = {p["adversario"]: p for p in jogos["proximos_jogos"]}
    assert "Time C" in prox
    assert prox["Time C"]["horario"] == "15:00"
    assert prox["Time C"]["local"] == "Estadio"
```

NOTA: se algum valor esperado divergir por causa do recalculo de saldo no 01-001 (1200.0), ajustar o dict da fixture E os asserts juntos para baterem com a aritmetica real. A prova vale quando os numeros do payload batem com a soma manual do seed.
</action>
<verify>
<automated>cd backend && python -m pytest tests/test_portal.py -q -k "not privacidade"</automated>
</verify>
<done>Todos os testes funcionais (200 sem token, meta, caixa saldo/totais, fluxo<=12 asc, atrasos int, evento liquido/filtro, jogos resumo/rankings/resultados/proximos) passam. `cd backend && python -m pytest tests/test_portal.py -q` verde para os funcionais.</done>
</task>

<task id="2" type="auto">
<files>backend/tests/test_portal.py</files>
<action>
Adicionar ao fim de `backend/tests/test_portal.py` o teste da TRAVA DE PRIVACIDADE (SEC-02/SEC-03). E o teste mais importante da fase: varre o JSON inteiro e garante que (a) nenhum nome de jogador aparece FORA de `jogos.*`, (b) nao existe chave de PII/registro cru em lugar nenhum, (c) atrasos sao int.

Logica: serializar o payload, remover o sub-objeto `jogos` (onde nome de jogador e legitimo em rankings, e adversario e nome de time), e varrer recursivamente TODO o resto (`meta`, `caixa`, `eventos`) procurando qualquer nome de jogador do seed. Se achar, falha.

```python
# --- SEC-02 / SEC-03: trava de privacidade (varredura do payload) ---

def _coletar_strings(obj):
    """Coleta recursivamente todas as strings de um objeto JSON (dict/list/str)."""
    out = []
    if isinstance(obj, dict):
        for v in obj.values():
            out.extend(_coletar_strings(v))
    elif isinstance(obj, list):
        for v in obj:
            out.extend(_coletar_strings(v))
    elif isinstance(obj, str):
        out.append(obj)
    return out


def _coletar_chaves(obj):
    """Coleta recursivamente todas as chaves de dict de um objeto JSON."""
    out = []
    if isinstance(obj, dict):
        for k, v in obj.items():
            out.append(k)
            out.extend(_coletar_chaves(v))
    elif isinstance(obj, list):
        for v in obj:
            out.extend(_coletar_chaves(v))
    return out


def test_privacidade_nenhum_nome_de_jogador_fora_de_jogos(client, seed_portal_data):
    exp = seed_portal_data
    body = client.get("/api/portal").json()

    # remove o bloco jogos: nome de jogador em ranking e nome de adversario (time) sao permitidos la
    sem_jogos = {k: v for k, v in body.items() if k != "jogos"}
    strings_financeiras = _coletar_strings(sem_jogos)
    blob = " ".join(strings_financeiras)

    for nome in exp["nomes_jogadores"]:   # ["Carlao", "Pedrinho"]
        assert nome not in blob, (
            f"VAZAMENTO: nome de jogador '{nome}' apareceu em contexto financeiro: {strings_financeiras}"
        )


def test_privacidade_sem_chaves_de_pii_nem_registro_cru(client, seed_portal_data):
    body = client.get("/api/portal").json()
    chaves = set(_coletar_chaves(body))
    proibidas = {
        "telefone", "transacoes", "participantes", "mensalidades_lista",
        "jogador_id", "valor_pago", "data_pagamento", "forma_pagto",
        "nome_avulso", "apelido", "password_hash", "comprovante",
    }
    vazadas = chaves & proibidas
    assert not vazadas, f"chaves de PII/registro cru no payload: {vazadas}"


def test_privacidade_atrasos_sao_int_nunca_lista(client, seed_portal_data):
    atrasos = client.get("/api/portal").json()["caixa"]["atrasos"]
    assert set(atrasos.keys()) == {"mensalidades", "jogadores"}
    assert isinstance(atrasos["mensalidades"], int) and not isinstance(atrasos["mensalidades"], bool)
    assert isinstance(atrasos["jogadores"], int) and not isinstance(atrasos["jogadores"], bool)
    # nenhum valor de atrasos pode ser lista/dict
    assert not isinstance(atrasos["mensalidades"], (list, dict))
    assert not isinstance(atrasos["jogadores"], (list, dict))
```

NOTA importante sobre falso-positivo: o seed usa nomes ("Carlao", "Pedrinho") que tambem aparecem em `jogos.artilharia` (legitimo). Por isso o teste REMOVE `jogos` antes de varrer. Se o handler vazasse, por ex, o nome do jogador devedor no bloco `caixa.atrasos`, o teste pegaria. Garantir que o seed NAO usa um nome de jogador que coincida com um titulo de evento ou nome de conta (senao falso-positivo). Os nomes do seed (Carlao/Pedrinho) nao colidem com contas (Caixa/Banco) nem eventos (Galeto Junho), entao a varredura e segura.
</action>
<verify>
<automated>cd backend && python -m pytest tests/test_portal.py -q -k "privacidade"</automated>
</verify>
<done>Os 3 testes de privacidade passam: nenhum nome de jogador do seed aparece em `meta`/`caixa`/`eventos`; nenhuma chave de PII/registro cru existe no payload; `atrasos` sao `int` (nao bool, nao lista). A suite inteira `cd backend && python -m pytest tests/test_portal.py -q` fica verde.</done>
</task>

## Criterios de Sucesso

- [ ] `pytest tests/test_portal.py -q` passa inteiro (funcionais + privacidade)
- [ ] Prova 200 sem header Authorization (API-01)
- [ ] Prova shape do payload e sub-blocos (API-02)
- [ ] Prova saldo_atual = soma manual e fluxo_12m <= 12 asc (API-03)
- [ ] Prova liquido = arrecadado - custo com custo_origem 'real'; cancelado/planejado-vazio ausentes (API-04, API-07)
- [ ] Prova resumo/rankings/resultados/proximos (API-05)
- [ ] Prova trava de privacidade: nenhum nome fora de jogos.*, sem PII, atrasos int (SEC-01, SEC-02, SEC-03)
