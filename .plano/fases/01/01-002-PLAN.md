---
phase: 01-backend-api-publica
plan: 01-002
type: feature
autonomous: true
wave: 1
depends_on: ["01-001"]
requirements: [API-01, API-02, API-03, API-04, API-05, API-07, SEC-01, SEC-02, SEC-03, DEPLOY-02]
must_haves:
  truths:
    - "GET /api/portal responde 200 SEM header Authorization (router publico, sem Depends(get_current_user))"
    - "O payload tem as 4 chaves de topo meta/caixa/eventos/jogos e valida contra PortalResponse"
    - "caixa.saldo_atual = soma de _calcular_saldo_atual das contas ativas; fluxo_12m <= 12 itens"
    - "Cada evento traz liquido = arrecadado - custo com custo_origem correto; cancelado nao aparece"
    - "atrasos sao COUNTs inteiros; nenhum nome de jogador fora de jogos.*; CORS inalterado"
  artifacts:
    - path: "backend/routers/portal.py"
      provides: "APIRouter publico /api/portal com handler agregador read-only"
    - path: "backend/main.py"
      provides: "Registro do portal.router via app.include_router (CORS intacto)"
  key_links:
    - from: "routers/portal.py"
      to: "routers/contas.py::_calcular_saldo_atual"
      via: "import e soma sobre contas ativas"
    - from: "routers/portal.py"
      to: "routers/jogos.py::_parse_entries"
      via: "import para montar rankings"
    - from: "main.py"
      to: "portal.router"
      via: "app.include_router(portal.router)"
---

# Fase 01 Plano 002: Router publico /api/portal (handler agregador)

**Objetivo:** Criar `backend/routers/portal.py` com um unico endpoint `GET /api/portal`, publico (sem `Depends(get_current_user)`), read-only, que monta o pacote agregado inteiro (meta + caixa + eventos + jogos) numa request, reusando a logica existente (`_calcular_saldo_atual`, `_parse_entries`) e aplicando a trava de privacidade dentro do handler. Registrar em `main.py` sem tocar no CORS. Depende do plano 01-001 (schemas `Portal*` ja existem).

## Contexto

@backend/routers/contas.py - `_calcular_saldo_atual(db, conta) -> float` (linha 14): `conta.saldo_inicial + sum(entradas) - sum(saidas)` via `func.coalesce(func.sum(Transacao.valor), 0)`. Importar esta funcao, NAO reimplementar.
@backend/routers/financeiro.py - `balanco` (linha 74): usa `mes_atual = now.strftime("%Y-%m")` e filtra `Transacao.data.like(f"{mes_atual}%")`. `fluxo_mensal` (linha 128): agrupa por `t.data[:7]`, soma por tipo, ordena. Replicar a regra (nao chamar o endpoint).
@backend/routers/jogos.py - `estatisticas` (linha 37): vitorias/empates/derrotas sobre `Jogo.realizado==1`, gols_marcados/gols_sofridos. `_parse_entries(text)` (linha 56): parseia "Nome (N), Outro" -> dict. `rankings` (linha 82): usa `_parse_entries` sobre `gols_descricao`/`assistencias` e conta `destaque`. Importar `_parse_entries`, replicar a logica de estatisticas/rankings inline (sao calculos simples sobre a mesma query).
@backend/routers/eventos.py - `resumo_evento` (linha 639): `valor_arrecadado = sum(p.valor_pago or 0 for p in parts)`. Replicar essa conta de arrecadado.
@backend/models.py - Evento.status SEM acento (planejado|em_andamento|concluido|cancelado). Mensalidade.status='atrasado'. Configuracao(chave, valor). Jogo(realizado 0/1, data, adversario, gols_favor, gols_contra, horario, local).
@backend/main.py - linha 12 import de routers; linhas 39-51 os `app.include_router(...)`; CORS nas linhas 30-36 (allow_origins localhost:3000/127.0.0.1:3000). NAO mexer no CORS.
@backend/schemas.py - models Portal* criados no plano 01-001 (PortalResponse e filhos).

## Tarefas

<task id="1" type="auto">
<files>backend/routers/portal.py</files>
<action>
Criar `backend/routers/portal.py` (NOVO). Router PUBLICO: `router = APIRouter(prefix="/api/portal", tags=["portal"])` SEM `dependencies=[Depends(get_current_user)]` (esta ausencia E a decisao de design que torna a rota publica).

Imports exatos no topo:
```python
from datetime import datetime
from collections import defaultdict

from fastapi import APIRouter, Depends
from sqlalchemy.orm import Session
from sqlalchemy import func

from database import get_db
from models import Conta, Transacao, Mensalidade, Evento, EventoParticipante, Jogo, Configuracao
from routers.contas import _calcular_saldo_atual
from routers.jogos import _parse_entries
from schemas import (
    PortalResponse, PortalMeta, PortalCaixa, PortalCaixaAtrasos, PortalFluxoMes,
    PortalEvento, PortalJogos, PortalJogoResumo, PortalRankingEntry,
    PortalResultado, PortalProximoJogo,
)

router = APIRouter(prefix="/api/portal", tags=["portal"])
```

Definir helpers privados (montadores de cada bloco) + o handler. NENHUM `db.commit()`, `db.add()`, `db.delete()` em nenhum lugar (read-only).

### Helper 1: `_montar_caixa(db) -> PortalCaixa`
```python
def _montar_caixa(db: Session) -> PortalCaixa:
    now = datetime.now()
    mes_atual = now.strftime("%Y-%m")

    # saldo_atual = soma de _calcular_saldo_atual das contas ativas
    contas = db.query(Conta).filter(Conta.ativo == 1).all()
    saldo_atual = sum(_calcular_saldo_atual(db, c) for c in contas)

    total_entrou = db.query(func.coalesce(func.sum(Transacao.valor), 0)).filter(
        Transacao.tipo == "entrada").scalar()
    total_saiu = db.query(func.coalesce(func.sum(Transacao.valor), 0)).filter(
        Transacao.tipo == "saida").scalar()

    entrou_mes = db.query(func.coalesce(func.sum(Transacao.valor), 0)).filter(
        Transacao.tipo == "entrada", Transacao.data.like(f"{mes_atual}%")).scalar()
    saiu_mes = db.query(func.coalesce(func.sum(Transacao.valor), 0)).filter(
        Transacao.tipo == "saida", Transacao.data.like(f"{mes_atual}%")).scalar()

    # fluxo dos ultimos 12 meses (ordem cronologica asc), replicando financeiro.fluxo_mensal
    transacoes = db.query(Transacao).order_by(Transacao.data).all()
    fluxo: dict[str, dict[str, float]] = {}
    for t in transacoes:
        mes = t.data[:7] if t.data else "unknown"
        if mes == "unknown":
            continue
        bucket = fluxo.setdefault(mes, {"entradas": 0.0, "saidas": 0.0})
        if t.tipo == "entrada":
            bucket["entradas"] += t.valor
        else:
            bucket["saidas"] += t.valor
    ult_12 = sorted(fluxo.keys())[-12:]  # cronologico asc, max 12
    fluxo_12m = [
        PortalFluxoMes(mes=m, entradas=fluxo[m]["entradas"], saidas=fluxo[m]["saidas"])
        for m in ult_12
    ]

    # atrasos (privacidade: COUNT puro, sem nomes)
    n_mensalidades = db.query(func.count(Mensalidade.id)).filter(
        Mensalidade.status == "atrasado",
        Mensalidade.mes_referencia == mes_atual,
    ).scalar()
    n_jogadores = db.query(func.count(func.distinct(Mensalidade.jogador_id))).filter(
        Mensalidade.status == "atrasado",
        Mensalidade.mes_referencia == mes_atual,
    ).scalar()

    return PortalCaixa(
        saldo_atual=float(saldo_atual),
        total_entrou=float(total_entrou),
        total_saiu=float(total_saiu),
        entrou_mes=float(entrou_mes),
        saiu_mes=float(saiu_mes),
        fluxo_12m=fluxo_12m,
        atrasos=PortalCaixaAtrasos(
            mensalidades=int(n_mensalidades or 0),
            jogadores=int(n_jogadores or 0),
        ),
    )
```

### Helper 2: `_montar_eventos(db) -> list[PortalEvento]`
Filtro (API-07): incluir status em ('concluido','em_andamento'); 'planejado' so se arrecadado>0; excluir 'cancelado'. Ordenar por data_inicio desc (None por ultimo).
```python
def _montar_eventos(db: Session) -> list[PortalEvento]:
    candidatos = db.query(Evento).filter(
        Evento.status.in_(["concluido", "em_andamento", "planejado"])
    ).all()

    out: list[PortalEvento] = []
    for e in candidatos:
        arrecadado = db.query(func.coalesce(func.sum(EventoParticipante.valor_pago), 0)).filter(
            EventoParticipante.evento_id == e.id
        ).scalar()
        arrecadado = float(arrecadado or 0)

        # planejado so entra se arrecadou; cancelado ja foi excluido na query
        if e.status == "planejado" and arrecadado <= 0:
            continue

        # regra de custo/custo_origem
        if e.custo_real and e.custo_real > 0:
            custo, custo_origem = float(e.custo_real), "real"
        elif e.custo_estimado and e.custo_estimado > 0:
            custo, custo_origem = float(e.custo_estimado), "estimado"
        else:
            custo, custo_origem = 0.0, "sem_custo"

        out.append(PortalEvento(
            titulo=e.titulo,
            tipo=e.tipo,
            data=e.data_inicio,
            arrecadado=arrecadado,
            custo=custo,
            custo_origem=custo_origem,
            liquido=arrecadado - custo,
            status=e.status,
        ))

    # ordenar por data desc; None vai pro fim
    out.sort(key=lambda ev: (ev.data is None, ev.data or ""), reverse=False)
    out.sort(key=lambda ev: ev.data or "", reverse=True)
    return out
```
NOTA de ordenacao: o objetivo e data_inicio desc com None por ultimo. Implementar de forma simples e correta: separar os com data dos sem data, ordenar os com data desc, e concatenar os sem data no fim. Se a dupla-chamada de sort acima nao garantir None-por-ultimo, usar:
```python
com_data = sorted([e for e in out if e.data], key=lambda e: e.data, reverse=True)
sem_data = [e for e in out if not e.data]
return com_data + sem_data
```

### Helper 3: `_montar_jogos(db) -> PortalJogos`
Replicar estatisticas + rankings (sobre realizado==1) e derivar ultimos_resultados/proximos_jogos.
```python
def _montar_jogos(db: Session) -> PortalJogos:
    realizados = db.query(Jogo).filter(Jogo.realizado == 1).all()

    vitorias = sum(1 for j in realizados if j.gols_favor > j.gols_contra)
    empates = sum(1 for j in realizados if j.gols_favor == j.gols_contra)
    derrotas = sum(1 for j in realizados if j.gols_favor < j.gols_contra)
    gols_pro = sum(j.gols_favor for j in realizados)
    gols_contra = sum(j.gols_contra for j in realizados)

    artilharia: dict[str, int] = defaultdict(int)
    assist: dict[str, int] = defaultdict(int)
    destaques: dict[str, int] = defaultdict(int)
    for j in realizados:
        for nome, qtd in _parse_entries(j.gols_descricao or "").items():
            artilharia[nome] += qtd
        for nome, qtd in _parse_entries(j.assistencias or "").items():
            assist[nome] += qtd
        if j.destaque and j.destaque.strip():
            destaques[j.destaque.strip()] += 1

    def _rank(d: dict[str, int]) -> list[PortalRankingEntry]:
        return sorted(
            [PortalRankingEntry(nome=k, quantidade=v) for k, v in d.items()],
            key=lambda r: r.quantidade, reverse=True,
        )

    # ultimos resultados: realizados, data desc, limite 10
    ult = sorted(realizados, key=lambda j: j.data or "", reverse=True)[:10]
    ultimos_resultados = [
        PortalResultado(
            data=j.data, adversario=j.adversario,
            placar=f"{j.gols_favor}x{j.gols_contra}",
        ) for j in ult
    ]

    # proximos jogos: realizado=0 e data >= hoje, data asc
    hoje = datetime.now().strftime("%Y-%m-%d")
    futuros = db.query(Jogo).filter(
        Jogo.realizado == 0, Jogo.data >= hoje
    ).order_by(Jogo.data.asc()).all()
    proximos_jogos = [
        PortalProximoJogo(
            data=j.data, horario=j.horario, local=j.local, adversario=j.adversario,
        ) for j in futuros
    ]

    return PortalJogos(
        resumo=PortalJogoResumo(
            vitorias=vitorias, empates=empates, derrotas=derrotas,
            gols_pro=gols_pro, gols_contra=gols_contra,
        ),
        artilharia=_rank(artilharia),
        assistencias=_rank(assist),
        destaques=_rank(destaques),
        ultimos_resultados=ultimos_resultados,
        proximos_jogos=proximos_jogos,
    )
```

### Helper 4: `_montar_meta(db) -> PortalMeta`
```python
def _montar_meta(db: Session) -> PortalMeta:
    cfg = db.query(Configuracao).filter(Configuracao.chave == "time_nome").first()
    time_nome = (cfg.valor if cfg and cfg.valor else "Velhos Parceiros F.C.")
    return PortalMeta(
        time_nome=time_nome,
        atualizado_em=datetime.now().isoformat(),
    )
```

### Handler
```python
@router.get("", response_model=PortalResponse)
def portal(db: Session = Depends(get_db)):
    return PortalResponse(
        meta=_montar_meta(db),
        caixa=_montar_caixa(db),
        eventos=_montar_eventos(db),
        jogos=_montar_jogos(db),
    )
```

REGRAS DURAS:
- SEC-02: `_montar_caixa` e `_montar_eventos` NUNCA selecionam `Jogador.nome`. Nome de jogador so aparece em `_montar_jogos` (rankings, via `_parse_entries` sobre texto livre de gols/assist/destaque) e em `adversario` (que e nome de time, nao PII).
- SEC-01/SEC-03: atrasos sao COUNT/COUNT DISTINCT inteiros; nenhuma lista de Mensalidade/Transacao/EventoParticipante e serializada.
- Read-only: confirmar que o arquivo inteiro NAO tem `db.commit`/`db.add`/`db.delete`.
</action>
<verify>
<automated>cd backend && python -c "import ast,sys; src=open('routers/portal.py').read(); tree=ast.parse(src); calls=[n.attr for n in ast.walk(tree) if isinstance(n, ast.Attribute) and n.attr in ('commit','add','delete')]; assert not calls, f'router nao pode escrever no banco: {calls}'; assert 'get_current_user' not in src, 'router deve ser publico (sem get_current_user)'; assert 'prefix=\"/api/portal\"' in src; print('OK portal.py read-only e publico')"</automated>
</verify>
<done>`routers/portal.py` existe, importa sem erro, define `router` com prefix `/api/portal` sem `get_current_user`, e o handler `portal` retorna `PortalResponse`. Nenhuma chamada de escrita (commit/add/delete). `python -c "from routers.portal import router"` roda dentro de `backend/`.</done>
</task>

<task id="2" type="auto">
<files>backend/main.py</files>
<action>
Registrar o router publico em `backend/main.py`, SEM tocar no CORS nem em mais nada.

1. Na linha 12 (`from routers import auth, jogadores, ... , campanhas`), adicionar `portal` a lista de imports. Resultado:
```python
from routers import auth, jogadores, mensalidades, financeiro, eventos, jogos, cartoes, promocoes, whatsapp, dashboard, configuracoes, contas, campanhas, portal
```

2. No bloco de `app.include_router(...)` (linhas ~39-51), adicionar uma linha junto aos demais (apos `app.include_router(campanhas.router)` ou em qualquer ponto do bloco):
```python
app.include_router(portal.router)
```

NAO alterar o middleware CORS (linhas 30-36: `allow_origins=["http://localhost:3000", "http://127.0.0.1:3000"]` permanece identico). NAO adicionar env, migration nem container. Confirmar que nao existe nenhum `Depends(get_current_user)` global no `app` (a auth e por-router; o portal fica publico por nao herdar nenhuma dependencia global).
</action>
<verify>
<automated>cd backend && python -c "src=open('main.py').read(); assert 'portal' in src.split('from routers import')[1].split(chr(10))[0], 'portal nao importado'; assert 'app.include_router(portal.router)' in src, 'portal.router nao registrado'; assert 'allow_origins=[\"http://localhost:3000\", \"http://127.0.0.1:3000\"]' in src, 'CORS foi alterado'; print('OK main.py registra portal e CORS intacto')"</automated>
</verify>
<done>`main.py` importa `portal` e chama `app.include_router(portal.router)`. A allowlist de CORS permanece exatamente `["http://localhost:3000", "http://127.0.0.1:3000"]`. Nenhuma migration nova foi adicionada.</done>
</task>

<task id="3" type="auto">
<files>backend/routers/portal.py</files>
<action>
Prova de integracao manual (smoke do app inteiro): subir a app de teste em memoria e bater no endpoint SEM token, confirmando 200 e as 4 chaves de topo. NAO criar arquivo novo aqui (a suite completa e o plano 01-003); rodar um one-liner que importa a app real com overrides minimos. Comando de prova abaixo em <verify>. Se ele falhar, corrigir o handler ate passar. Esta tarefa nao altera codigo se as tarefas 1 e 2 ja estiverem corretas; ela apenas EXIGE a prova de que o endpoint responde 200 sem auth e tem o shape certo, fechando API-01/API-02 antes de seguir.
</action>
<verify>
<automated>cd backend && python -c "
import main
main.start_scheduler = lambda *a, **k: None
from fastapi.testclient import TestClient
from database import Base, get_db
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from sqlalchemy.pool import StaticPool
import models
eng = create_engine('sqlite://', connect_args={'check_same_thread': False}, poolclass=StaticPool)
Base.metadata.create_all(bind=eng)
S = sessionmaker(bind=eng)
from main import app
def odb():
    d = S();
    try: yield d
    finally: d.close()
app.dependency_overrides[get_db] = odb
with TestClient(app) as c:
    r = c.get('/api/portal')  # SEM header Authorization
    assert r.status_code == 200, (r.status_code, r.text)
    body = r.json()
    assert set(body.keys()) == {'meta','caixa','eventos','jogos'}, body.keys()
    assert isinstance(body['caixa']['atrasos']['mensalidades'], int)
    print('OK 200 sem token, 4 chaves de topo, atrasos int')
"</automated>
</verify>
<done>`GET /api/portal` retorna 200 SEM header Authorization, com exatamente as 4 chaves de topo (meta, caixa, eventos, jogos) e `caixa.atrasos.mensalidades` inteiro. Banco vazio nao quebra (coalesce/0 em todos os SUMs).</done>
</task>

## Criterios de Sucesso

- [ ] `GET /api/portal` responde 200 SEM token (API-01)
- [ ] Payload tem as 4 chaves de topo e valida contra `PortalResponse` (API-02, API-06)
- [ ] `caixa.saldo_atual` = soma de `_calcular_saldo_atual` das contas ativas; `fluxo_12m` <= 12 itens (API-03)
- [ ] Cada evento traz `liquido = arrecadado - custo` com `custo_origem` correto; `cancelado` ausente; `planejado` sem arrecadacao ausente (API-04, API-07)
- [ ] `resumo`/rankings batem com a logica de jogos.py (API-05)
- [ ] `atrasos` sao COUNTs inteiros; nenhum nome fora de `jogos.*`; sem PII (SEC-01, SEC-02, SEC-03)
- [ ] CORS inalterado; sem migration (DEPLOY-02)
- [ ] `portal.py` e read-only (sem commit/add/delete)
