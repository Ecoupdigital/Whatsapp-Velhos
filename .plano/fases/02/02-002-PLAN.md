---
phase: 02-frontend-portal
plan: 02-002
type: feature
autonomous: true
wave: 1
depends_on: [02-001]
requirements: [UI-01, UI-02, DEPLOY-01, DEPLOY-02]
must_haves:
  truths:
    - "A rota /transparencia abre SEM login e NAO redireciona pro /login"
    - "O layout (public) renderiza header limpo (escudo + nome) sem sidebar e sem guard de auth"
    - "O HTML servido em /transparencia contem meta tag noindex (robots index:false, follow:false)"
    - "A pagina busca /api/portal e trata loading (skeleton) e erro (mensagem amigavel)"
  artifacts:
    - path: "frontend/src/app/(public)/layout.tsx"
      provides: "Layout publico server component: header escudo+nome, metadata robots noindex, sem guard"
    - path: "frontend/src/app/(public)/transparencia/page.tsx"
      provides: "Pagina client: fetchPortal no mount, estados loading/erro, monta os 4 blocos (placeholders nesta fase)"
  key_links:
    - from: "(public)/transparencia/page.tsx"
      to: "lib/portal.ts fetchPortal()"
      via: "useEffect no mount, useState pra data/loading/erro"
    - from: "(public)/layout.tsx metadata.robots"
      to: "<head> noindex"
      via: "export const metadata robots index:false follow:false (server component)"
---

# Fase 02 Plano 002: Route group (public) + scaffold da pagina /transparencia

**Objetivo:** Criar o route group publico `(public)` FORA do `(app)` (que tem guard de auth),
com layout server component (header limpo escudo+nome, metadata noindex) e a pagina
`transparencia/page.tsx` como client component que faz `fetchPortal()` no mount, controla os
estados loading/erro, e ja deixa o esqueleto dos 4 blocos pronto pra receber os componentes dos
planos 003-006. Resolve UI-01 (sem guard), DEPLOY-01 (noindex) e a casca de UI-02.

## Contexto

@frontend/src/app/(app)/layout.tsx - layout PROTEGIDO: client component com `useAuth()` + `router.replace("/login")`. O `(public)` NAO pode importar nada disso. Referencia do que NAO fazer.
@frontend/src/app/layout.tsx - root layout: ja injeta fonts (oswald/dm-sans/jetbrains), forca `dark` no <html>, body `bg-surface-primary text-txt-primary font-body`. O `(public)/layout.tsx` herda isso; NAO redefinir <html>/<body>.
@frontend/src/lib/portal.ts - `fetchPortal()` e `formatAtualizadoEm()` (do plano 001).
@frontend/src/types/portal.ts - `PortalResponse` (do plano 001).
@frontend/src/components/ui/Skeleton.tsx - `Skeleton`, `SkeletonCard` (reusar no loading).
@frontend/public/icons/icon-192.svg - escudo VP (circulo vermelho #E31E24 com "VP" branco). Usar via `<img>` ou `next/image`.

## Dependencia de fase

Plano 001 deve estar feito (tipos + fetchPortal). Fase 01 (backend) deve responder `/api/portal`
pra prova de runtime; sem ela a pagina sobe mas cai no estado de erro (que tambem e testavel).

## Tarefas

<task id="1" type="auto">
<files>frontend/src/app/(public)/layout.tsx</files>
<action>
Criar o layout do route group publico. SERVER COMPONENT (sem "use client") pra poder exportar
`metadata` com noindex. Header limpo: escudo (icon-192.svg) + nome "Velhos Parceiros F.C.".
SEM Sidebar, SEM MobileNav, SEM useAuth, SEM redirect. Nao redefinir <html>/<body> (vem do root).

Conteudo EXATO:

```tsx
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Prestacao de Contas | Velhos Parceiros F.C.",
  description: "Transparencia financeira e esportiva do Velhos Parceiros F.C.",
  robots: { index: false, follow: false },
};

export default function PublicLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div className="min-h-screen bg-surface-primary text-txt-primary">
      <header className="border-b border-border-subtle bg-surface-secondary/60 backdrop-blur sticky top-0 z-10">
        <div className="mx-auto flex max-w-2xl items-center gap-3 px-4 py-3">
          {/* eslint-disable-next-line @next/next/no-img-element */}
          <img
            src="/icons/icon-192.svg"
            alt="Escudo Velhos Parceiros F.C."
            className="h-9 w-9 rounded-lg"
            width={36}
            height={36}
          />
          <span className="font-display text-base font-semibold tracking-tight text-txt-primary">
            Velhos Parceiros F.C.
          </span>
        </div>
      </header>
      <main className="mx-auto max-w-2xl px-4 pb-16 pt-4">{children}</main>
    </div>
  );
}
```

Notas:
- `robots: { index: false, follow: false }` cobre DEPLOY-01 pra TODA rota dentro de `(public)`.
- `max-w-2xl` mantem mobile-first: coluna unica centrada, boa leitura no celular (abrem pelo WhatsApp).
- Header sticky com `backdrop-blur` reusa tokens reais (`border-border-subtle`, `surface.secondary`).
- Usar `<img>` (com o comentario eslint-disable) evita config de next/image pro SVG estatico.
</action>
<verify>
<automated>cd frontend && test -f "src/app/(public)/layout.tsx" && grep -q "robots" "src/app/(public)/layout.tsx" && grep -q "index: false" "src/app/(public)/layout.tsx" && grep -q "icon-192.svg" "src/app/(public)/layout.tsx" && ! grep -q "useAuth\|use client\|router.replace" "src/app/(public)/layout.tsx" && echo "OK layout publico sem guard com noindex"</automated>
</verify>
<done>`(public)/layout.tsx` existe, e server component (sem "use client"), exporta `metadata.robots` com index:false/follow:false, mostra escudo + nome, e NAO contem useAuth/redirect.</done>
</task>

<task id="2" type="auto">
<files>frontend/src/app/(public)/transparencia/page.tsx</files>
<action>
Criar a pagina como CLIENT COMPONENT (precisa de useEffect/useState pro fetch). Faz `fetchPortal()`
no mount, controla `data | null`, `loading`, `erro`. Renderiza:
- loading: bloco de skeletons (reusar `SkeletonCard` do ui).
- erro: mensagem amigavel pt-BR + botao "Tentar de novo" que refaz o fetch.
- sucesso: por enquanto, placeholders dos 4 blocos (planos 003-006 substituem). Cada placeholder e
  uma `<section>` com data-block pra prova visual saber que o slot existe.

Conteudo EXATO (os imports de blocos reais virao nos proximos planos; aqui ficam comentados/TODO):

```tsx
"use client";

import { useCallback, useEffect, useState } from "react";
import { fetchPortal } from "@/lib/portal";
import type { PortalResponse } from "@/types/portal";
import { SkeletonCard } from "@/components/ui";
// Blocos (preenchidos nos planos 003-006):
// import { HeroCaixa } from "@/components/portal/HeroCaixa";
// import { CaixaBloco } from "@/components/portal/CaixaBloco";
// import { EventosBloco } from "@/components/portal/EventosBloco";
// import { JogosBloco } from "@/components/portal/JogosBloco";

export default function TransparenciaPage() {
  const [data, setData] = useState<PortalResponse | null>(null);
  const [loading, setLoading] = useState(true);
  const [erro, setErro] = useState<string | null>(null);

  const carregar = useCallback(async () => {
    setLoading(true);
    setErro(null);
    try {
      const portal = await fetchPortal();
      setData(portal);
    } catch (e) {
      setErro(
        e instanceof Error
          ? "Nao foi possivel carregar a prestacao de contas agora. Tente de novo em instantes."
          : "Erro inesperado."
      );
    } finally {
      setLoading(false);
    }
  }, []);

  useEffect(() => {
    carregar();
  }, [carregar]);

  if (loading) {
    return (
      <div className="space-y-6" data-state="loading">
        <SkeletonCard className="h-40" />
        <SkeletonCard className="h-64" />
        <SkeletonCard className="h-48" />
        <SkeletonCard className="h-56" />
      </div>
    );
  }

  if (erro || !data) {
    return (
      <div
        className="flex flex-col items-center gap-4 py-16 text-center"
        data-state="error"
      >
        <p className="font-body text-sm text-txt-secondary">{erro ?? "Sem dados."}</p>
        <button
          onClick={carregar}
          className="rounded-lg bg-brand-red px-4 py-2 font-body text-sm font-medium text-white transition-colors hover:bg-brand-red-hover"
        >
          Tentar de novo
        </button>
      </div>
    );
  }

  return (
    <div className="space-y-8" data-state="ready">
      {/* PLANO 003: <HeroCaixa meta={data.meta} caixa={data.caixa} /> */}
      <section data-block="hero" className="text-txt-secondary">[hero]</section>
      {/* PLANO 003 + 004: <CaixaBloco caixa={data.caixa} /> com <FluxoChart /> */}
      <section data-block="caixa" className="text-txt-secondary">[caixa]</section>
      {/* PLANO 005: <EventosBloco eventos={data.eventos} /> */}
      <section data-block="eventos" className="text-txt-secondary">[eventos]</section>
      {/* PLANO 006: <JogosBloco jogos={data.jogos} /> */}
      <section data-block="jogos" className="text-txt-secondary">[jogos]</section>

      <footer className="pt-6 text-center font-body text-xs text-txt-tertiary">
        Velhos Parceiros F.C. - prestacao de contas em tempo real
      </footer>
    </div>
  );
}
```

Notas:
- NAO usar `apiFetch` nem `api.get` (redirecionam em 401). Usar so `fetchPortal`.
- Os `data-block` e `data-state` sao ganchos pra prova visual/Playwright do plano 007.
- Plano 007 substitui os placeholders pelos componentes reais e adiciona estados vazios por bloco.
</action>
<verify>
<automated>cd frontend && test -f "src/app/(public)/transparencia/page.tsx" && grep -q "fetchPortal" "src/app/(public)/transparencia/page.tsx" && ! grep -q "apiFetch\|api.get\|useAuth" "src/app/(public)/transparencia/page.tsx" && grep -q "data-block=\"hero\"" "src/app/(public)/transparencia/page.tsx" && echo "OK page scaffold usa fetchPortal e tem slots dos 4 blocos"</automated>
</verify>
<done>`(public)/transparencia/page.tsx` existe, e client component, usa `fetchPortal` (nunca apiFetch/useAuth), tem estados loading/erro com botao de retry, e os 4 slots `data-block` (hero/caixa/eventos/jogos) + footer.</done>
</task>

<task id="3" type="auto">
<files>frontend/src/app/(public)/transparencia/page.tsx</files>
<action>
Prova de que a rota sobe sem guard e compila. Rodar build de tipo (`tsc --noEmit`) e lint do
route group. Confirmar que NADA do `(app)` (useAuth, Sidebar) vazou pro `(public)`. Este e um
gate rapido antes da prova visual do plano 007 (que sobe o dev server de verdade).
</action>
<verify>
<automated>cd frontend && npx tsc --noEmit 2>&1 | grep -iE "\(public\)" && exit 1 || echo "OK: route group (public) compila sem erro de tipo"</automated>
</verify>
<done>`tsc --noEmit` nao acusa nenhum erro nos arquivos de `(public)`. O route group compila isolado do guard de `(app)`.</done>
</task>

## Criterios de Sucesso

- [ ] `(public)/layout.tsx` e server component com `metadata.robots` noindex e header escudo+nome, sem guard
- [ ] `(public)/transparencia/page.tsx` e client component que usa `fetchPortal` (nunca apiFetch/useAuth)
- [ ] Estados loading (skeletons) e erro (mensagem + retry) implementados
- [ ] Os 4 slots `data-block` (hero/caixa/eventos/jogos) + footer presentes pra receber os blocos
- [ ] `tsc --noEmit` limpo pro route group; nenhuma importacao de Sidebar/useAuth no `(public)`
