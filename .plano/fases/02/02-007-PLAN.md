---
phase: 02-frontend-portal
plan: 02-007
type: feature
autonomous: true
wave: 3
depends_on: [02-003, 02-004, 02-005, 02-006]
requirements: [UI-01, UI-02, UI-03, UI-04, UI-05, DEPLOY-01, DEPLOY-02]
must_haves:
  truths:
    - "A pagina /transparencia monta os 4 blocos reais (Hero, Caixa+grafico, Eventos, Em campo) + footer"
    - "A pagina abre SEM login e nao redireciona pro /login (prova de runtime)"
    - "O <head> de /transparencia contem a meta tag noindex"
    - "O layout e usavel em viewport mobile e desktop (prova por screenshot Playwright)"
    - "Nenhuma chamada do navegador vai direto ao backend (same-origin via rewrite)"
  artifacts:
    - path: "frontend/src/app/(public)/transparencia/page.tsx"
      provides: "Pagina final com os 4 blocos reais montados (substitui placeholders do plano 002)"
  key_links:
    - from: "page.tsx"
      to: "HeroCaixa, CaixaBloco, EventosBloco, JogosBloco"
      via: "import do barrel @/components/portal, render condicionado a data carregada"
---

# Fase 02 Plano 007: Montagem final + estados + prova visual

**Objetivo:** Substituir os placeholders da `page.tsx` (plano 002) pelos 4 blocos reais (planos
003-006), garantir estados (loading/erro ja existem; vazios por bloco vivem nos componentes),
e PROVAR visualmente que tudo renderiza no mobile e desktop, que abre sem login, que o `<head>`
tem noindex e que o navegador fala same-origin. Fecha UI-01..05, DEPLOY-01, DEPLOY-02.

## Contexto

@frontend/src/app/(public)/transparencia/page.tsx - scaffold do plano 002 (placeholders `data-block`).
@frontend/src/components/portal/index.ts - barrel: HeroCaixa, CaixaBloco, FluxoChart, EventosBloco, JogosBloco.
@frontend/src/app/(public)/layout.tsx - layout com metadata noindex (plano 002).
@frontend/next.config.mjs - rewrite `/api/:path*` -> `${BACKEND_URL||http://backend:8000}/api/:path*`. Pro dev local, rodar com `BACKEND_URL` apontando pro backend da Fase 01.
@.plano/BRIEFING.md - layout dos 4 blocos numa rolagem.

## Dependencia de fase

CRITICA: planos 003-006 (componentes dos blocos) feitos. E **Fase 01 rodando** (backend
`/api/portal` respondendo) pra prova de runtime com dado real. Se o banco dev estiver vazio,
ver a tarefa de seed minimo (tarefa 2) antes da prova visual.

## Tarefas

<task id="1" type="auto">
<files>frontend/src/app/(public)/transparencia/page.tsx</files>
<action>
Substituir os 4 placeholders `data-block` pelos componentes reais. Trocar o bloco do estado
`ready` (a `<div data-state="ready">`) por:

```tsx
  return (
    <div className="space-y-8" data-state="ready">
      <HeroCaixa meta={data.meta} caixa={data.caixa} />
      <CaixaBloco caixa={data.caixa} />
      <EventosBloco eventos={data.eventos} />
      <JogosBloco jogos={data.jogos} />

      <footer className="pt-6 text-center font-body text-xs text-txt-tertiary">
        Velhos Parceiros F.C. - prestacao de contas em tempo real
      </footer>
    </div>
  );
```

E ajustar os imports no topo (descomentar/adicionar):
```tsx
import { HeroCaixa, CaixaBloco, EventosBloco, JogosBloco } from "@/components/portal";
```
Remover as linhas de import comentadas e os placeholders. Manter os estados loading (skeletons)
e erro (mensagem + retry) intactos. Os blocos ja trazem seus proprios `data-block` e estados
vazios internos (planos 003-006), entao a page nao precisa mais dos `<section data-block>` falsos.
</action>
<verify>
<automated>cd frontend && grep -q "import { HeroCaixa, CaixaBloco, EventosBloco, JogosBloco }" "src/app/(public)/transparencia/page.tsx" && grep -q "<HeroCaixa meta={data.meta} caixa={data.caixa} />" "src/app/(public)/transparencia/page.tsx" && grep -q "<JogosBloco jogos={data.jogos} />" "src/app/(public)/transparencia/page.tsx" && ! grep -q "\[hero\]\|\[caixa\]\|\[eventos\]\|\[jogos\]" "src/app/(public)/transparencia/page.tsx" && npx tsc --noEmit 2>&1 | grep -iE "\(public\)|portal/" && exit 1 || echo "OK page final monta 4 blocos"</automated>
</verify>
<done>`page.tsx` importa do barrel e renderiza `HeroCaixa`, `CaixaBloco`, `EventosBloco`, `JogosBloco` + footer; placeholders removidos; loading/erro intactos; `tsc --noEmit` limpo.</done>
</task>

<task id="2" type="auto">
<files>frontend/scripts/seed-portal-dev.sh</files>
<action>
Criar um script opcional de seed/verificacao do backend dev, pra a prova visual nao cair em "tudo
vazio". O script NAO mexe no schema; so checa se `/api/portal` responde e imprime se ha dado.
Se o backend da Fase 01 ja tem dado real (banco dev populado), o script so confirma. Se vazio, ele
documenta que os estados vazios dos blocos serao exercitados (que tambem e uma prova valida).

Conteudo:

```bash
#!/usr/bin/env bash
# Verifica o backend da Fase 01 antes da prova visual do portal.
# Uso: BACKEND_URL=http://localhost:8000 bash frontend/scripts/seed-portal-dev.sh
set -euo pipefail
BACKEND_URL="${BACKEND_URL:-http://localhost:8000}"
echo "Checando ${BACKEND_URL}/api/portal ..."
RESP="$(curl -s -o /dev/null -w '%{http_code}' "${BACKEND_URL}/api/portal" || echo 000)"
if [ "$RESP" != "200" ]; then
  echo "FALHA: /api/portal retornou HTTP ${RESP}. Suba a Fase 01 antes da prova visual."
  exit 1
fi
echo "OK: /api/portal responde 200 sem token."
BODY="$(curl -s "${BACKEND_URL}/api/portal")"
echo "$BODY" | head -c 400
echo ""
# Sinaliza se ha conteudo (so informativo; estados vazios sao validos).
echo "$BODY" | grep -q '"eventos":\[\]' && echo "[info] sem eventos -> bloco Eventos mostrara estado vazio" || true
echo "$BODY" | grep -q '"fluxo_12m":\[\]' && echo "[info] sem fluxo -> grafico mostrara estado vazio" || true
```

Tornar executavel. Este script e um GATE de dependencia da Fase 1, nao um seed destrutivo.
</action>
<verify>
<automated>cd frontend && test -f scripts/seed-portal-dev.sh && bash -n scripts/seed-portal-dev.sh && echo "OK script de gate valido (sintaxe)"</automated>
</verify>
<done>`frontend/scripts/seed-portal-dev.sh` existe, passa em `bash -n`, e checa o `/api/portal` (HTTP 200 sem token) antes da prova visual, sinalizando blocos vazios.</done>
</task>

<task id="3" type="auto">
<files>frontend/scripts/prova-visual-portal.md</files>
<action>
Documentar o procedimento de prova visual (a fase de build roda isto com Playwright). Criar um
roteiro reproduzivel. Conteudo:

```markdown
# Prova visual do Portal (/transparencia)

## Pre-requisitos
1. Fase 01 de pe: backend respondendo `/api/portal` (rodar `bash frontend/scripts/seed-portal-dev.sh`).
2. Frontend em dev apontando pro backend:
   ```bash
   cd frontend && BACKEND_URL=http://localhost:8000 npm run dev
   ```
   (o rewrite de next.config.mjs leva `/api` -> BACKEND_URL, same-origin)
3. Rota: http://localhost:3000/transparencia

## Pontos a verificar (Playwright)
- [ ] A rota NAO redireciona pro /login (URL final continua /transparencia). -> UI-01
- [ ] Os 4 blocos aparecem: `[data-block="hero"]`, `[data-block="caixa"]`,
      `[data-block="eventos"]`, `[data-block="jogos"]` presentes no DOM. -> UI-02
- [ ] Dentro de caixa, `[data-slot="fluxo"]` contem um `<svg>` do recharts (grafico real),
      OU o estado vazio "Sem movimentacao". -> UI-03
- [ ] O Hero mostra o texto "atualizado em" + um padrao DD/MM as HH:MM. -> UI-04
- [ ] Badges de atraso presentes (texto "em atraso" ou "Sem atrasos no mes"). -> UI-05
- [ ] `<head>` contem `<meta name="robots" content="noindex,nofollow">` (ou index:false). -> DEPLOY-01
- [ ] Todas as requests de rede vao pro mesmo host (localhost:3000), nenhuma direto ao backend. -> DEPLOY-02

## Screenshots
- Mobile: viewport 390x844 (iPhone 12), screenshot full-page -> prova-portal-mobile.png
- Desktop: viewport 1280x900, screenshot full-page -> prova-portal-desktop.png

## Snippet Playwright (referencia pra a fase de build)
```js
const { chromium, devices } = require('playwright');
(async () => {
  const browser = await chromium.launch();
  for (const [name, opts] of [
    ['mobile', devices['iPhone 12']],
    ['desktop', { viewport: { width: 1280, height: 900 } }],
  ]) {
    const ctx = await browser.newContext(opts);
    const page = await ctx.newPage();
    const reqs = [];
    page.on('request', r => reqs.push(new URL(r.url()).host));
    await page.goto('http://localhost:3000/transparencia', { waitUntil: 'networkidle' });
    // nao redirecionou:
    if (!page.url().endsWith('/transparencia')) throw new Error('redirecionou: ' + page.url());
    // 4 blocos:
    for (const b of ['hero','caixa','eventos','jogos'])
      await page.locator(`[data-block="${b}"]`).first().waitFor({ state: 'attached' });
    // noindex:
    const robots = await page.locator('head meta[name="robots"]').getAttribute('content');
    if (!robots || !/noindex/i.test(robots)) throw new Error('sem noindex: ' + robots);
    // same-origin (todas as requests no host localhost:3000):
    const externos = [...new Set(reqs)].filter(h => h && h !== 'localhost:3000');
    if (externos.length) throw new Error('request fora do same-origin: ' + externos.join(','));
    await page.screenshot({ path: `prova-portal-${name}.png`, fullPage: true });
    console.log(name, 'OK');
    await ctx.close();
  }
  await browser.close();
})();
```
```

Este arquivo e o roteiro que a etapa de build/verificacao executa. Nesta tarefa de planejamento o
artefato e o roteiro em si (o build roda o Playwright de verdade).
</action>
<verify>
<automated>cd frontend && test -f scripts/prova-visual-portal.md && grep -q "data-block" scripts/prova-visual-portal.md && grep -q "noindex" scripts/prova-visual-portal.md && grep -q "same-origin" scripts/prova-visual-portal.md && grep -q "iPhone 12" scripts/prova-visual-portal.md && echo "OK roteiro de prova visual"</automated>
</verify>
<done>`frontend/scripts/prova-visual-portal.md` documenta: como subir (npm run dev + BACKEND_URL), a rota, os 7 pontos a verificar (mapeados a UI-01..05/DEPLOY-01/02), screenshots mobile+desktop e o snippet Playwright.</done>
</task>

<task id="4" type="auto">
<files>frontend/src/app/(public)/transparencia/page.tsx</files>
<action>
Gate final de build estatico do route group inteiro, garantindo que a pagina compila integrando
todos os blocos. Rodar o build do Next em modo de checagem de tipos/lint do front (sem precisar do
backend; o fetch falha no build estatico mas a pagina e client component com fetch no mount, entao
o build NAO depende do backend). Confirmar ausencia de erro de tipo/import em todo o portal.

Se o `next build` completo for pesado pro ambiente, basta `tsc --noEmit` cobrindo todo `src/`.
</action>
<verify>
<automated>cd frontend && npx tsc --noEmit && echo "OK: tsc --noEmit limpo em todo o frontend (portal integrado)"</automated>
</verify>
<done>`tsc --noEmit` passa limpo em todo o `frontend/src`, com os 4 blocos integrados na page. Nenhum erro de tipo/import no portal.</done>
</task>

<task id="5" type="checkpoint:human-verify">
<files>frontend/scripts/prova-visual-portal.md</files>
<action>
Checkpoint de verificacao visual APOS a automacao. A etapa de build/verificacao ja tera gerado
`prova-portal-mobile.png` e `prova-portal-desktop.png` rodando o snippet Playwright do roteiro
(tarefa 3) contra `npm run dev` + Fase 01. Aqui o usuario (Jonathan) confere os 2 screenshots:
- mobile: os 4 blocos empilham, escudo + saldo herói no topo, grafico aparece, badges visiveis,
  cards de evento e jogos legiveis no celular (vao abrir pelo WhatsApp).
- desktop: layout centrado (max-w-2xl), nada estourado.
Se algo visual estiver quebrado (estouro, contraste, grafico cortado), abrir ajuste pontual.
Esta e a unica pausa humana da fase; tudo antes e autonomo.
</action>
<verify>
<automated>cd frontend && test -f scripts/prova-visual-portal.md && echo "Roteiro pronto; screenshots gerados pela etapa de build (prova-portal-mobile.png / prova-portal-desktop.png) aguardam conferencia humana"</automated>
</verify>
<done>Os screenshots mobile e desktop foram gerados pela automacao e conferidos: os 4 blocos renderizam corretos e responsivos, grafico visivel, sem estouro. Jonathan aprova o visual.</done>
</task>

## Criterios de Sucesso

- [ ] `page.tsx` monta os 4 blocos reais (Hero, Caixa+grafico, Eventos, Em campo) + footer
- [ ] `/transparencia` abre sem login (nao redireciona) - verificado no Playwright
- [ ] `<head>` contem meta noindex (DEPLOY-01) - verificado no Playwright
- [ ] Todas as requests same-origin (DEPLOY-02) - verificado no Playwright
- [ ] Screenshots mobile (390x844) e desktop (1280x900) gerados e aprovados
- [ ] `tsc --noEmit` limpo em todo o frontend; nenhuma lib nova
