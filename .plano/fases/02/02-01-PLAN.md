---
phase: 02-api
plan: 02-01
type: feature
autonomous: true
wave: 0
depends_on: []
requirements: [API-07, API-09]
must_haves:
  truths:
    - "Schemas FaixaCreate/FaixaUpdate/FaixaOut existem e validam"
    - "Schemas ItemTipo/ItensUpdate/ItemOut existem"
    - "EventoOut desserializa tipos_item (str JSON) para list[str]"
    - "ParticipanteOut expoe faixas e itens"
    - "Harness de teste (TestClient + auth override + sqlite in-memory) roda e passa um smoke test"
  artifacts:
    - path: "backend/schemas.py"
      provides: "Schemas Pydantic novos e alterados para faixas, itens, tipos_item, resumo"
    - path: "backend/tests/conftest.py"
      provides: "Fixtures de teste: client autenticado, db isolado, evento+participante seed"
    - path: "backend/tests/test_schemas_smoke.py"
      provides: "Smoke test confirmando harness e desserializacao de tipos_item"
  key_links:
    - from: "conftest.py"
      to: "app (main.py) + get_db + get_current_user"
      via: "dependency_overrides apontando para engine sqlite in-memory"
    - from: "EventoOut.tipos_item"
      to: "coluna eventos.tipos_item (Text JSON)"
      via: "field_validator(mode='before') com json.loads"
---

# Fase 02 Plano 01: Schemas + Harness de Teste

**Objetivo:** Definir todos os schemas Pydantic novos/alterados da Fase 2 (faixas, itens, tipos_item, resumo, ParticipanteOut) e montar a infraestrutura de teste (pytest + FastAPI TestClient com auth e DB isolados) que os planos 02-02..02-05 vao reutilizar. Sem isso nao ha como cumprir a regra Nyquist (TEST-02, TEST-03 dependem deste harness).

## Contexto

@backend/schemas.py — todos os schemas Pydantic; secao `# === Eventos ===` (L209-344) e o alvo
@backend/models.py — `Evento` (L92), `EventoParticipante` (L118); Fase 1 ja adicionou `EventoCartaoFaixa`, `EventoParticipanteItem`, coluna `eventos.tipos_item` e os relationships `faixas`/`itens`
@backend/main.py — `app` FastAPI, `Base.metadata.create_all`, routers incluidos
@backend/database.py — `engine`, `SessionLocal`, `Base`, `get_db`
@backend/auth.py — `get_current_user` (dependency a sobrescrever nos testes)

> Pre-condicao: Fase 1 entregue. Antes de implementar, confirme com grep que `EventoCartaoFaixa`, `EventoParticipanteItem` existem em `models.py` e que `EventoParticipante` tem os relationships `faixas` e `itens`. Se faltarem, PARE — Fase 1 nao foi concluida.

## Tarefas

<task id="1" type="auto">
<files>backend/schemas.py</files>
<action>
Adicionar import no topo do arquivo (apos `from typing import Optional`, L2):
```python
from pydantic import BaseModel, field_validator
import json
```
(O arquivo hoje so importa `BaseModel`. Garanta que `field_validator` e `json` entrem sem duplicar imports.)

Na secao `# === Eventos ===`, ANTES de `class ParticipanteOut`, adicionar os schemas de faixa e item:

```python
class FaixaCreate(BaseModel):
    sem_numero: bool = False
    numero_inicio: Optional[int] = None
    numero_fim: Optional[int] = None
    quantidade: Optional[int] = None  # usado so quando sem_numero=True

class FaixaUpdate(BaseModel):
    sem_numero: Optional[bool] = None
    numero_inicio: Optional[int] = None
    numero_fim: Optional[int] = None
    quantidade: Optional[int] = None

class FaixaOut(BaseModel):
    id: int
    evento_participante_id: int
    numero_inicio: Optional[int] = None
    numero_fim: Optional[int] = None
    quantidade: int
    sem_numero: bool
    created_at: Optional[str] = None

    @field_validator("sem_numero", mode="before")
    @classmethod
    def _coerce_sem_numero(cls, v):
        # banco guarda Integer 0/1; expor como bool
        if isinstance(v, int):
            return bool(v)
        return v

    class Config:
        from_attributes = True

class ItemTipo(BaseModel):
    tipo: str
    qtd_vendido: int = 0
    qtd_pedido: int = 0

class ItensUpdate(BaseModel):
    itens: list[ItemTipo] = []

class ItemOut(BaseModel):
    id: int
    tipo: str
    qtd_vendido: int
    qtd_pedido: int

    class Config:
        from_attributes = True

class ResumoItemTipo(BaseModel):
    tipo: str
    total_vendido: int
    total_pedido: int
```
</action>
<verify><automated>cd backend && python -c "from schemas import FaixaCreate, FaixaUpdate, FaixaOut, ItemTipo, ItensUpdate, ItemOut, ResumoItemTipo; FaixaOut(id=1, evento_participante_id=1, quantidade=12, sem_numero=1); print('ok')"</automated></verify>
<done>Os 7 schemas importam sem erro; `FaixaOut` aceita `sem_numero=1` (int) e expoe como bool.</done>
</task>

<task id="2" type="auto">
<files>backend/schemas.py</files>
<action>
Alterar schemas existentes de Evento e Participante e Resumo:

1. Em `EventoCreate` (L211), adicionar campo no final:
```python
    tipos_item: Optional[list[str]] = None
```

2. Em `EventoUpdate` (L228), adicionar:
```python
    tipos_item: Optional[list[str]] = None
```

3. Em `EventoOut` (L246), adicionar campo + validator que desserializa o Text JSON do banco:
```python
    tipos_item: Optional[list[str]] = None
```
e dentro da classe (antes de `class Config`):
```python
    @field_validator("tipos_item", mode="before")
    @classmethod
    def _parse_tipos_item(cls, v):
        # banco guarda Text JSON ('["cru","assado"]') ou None/""
        if v is None or v == "":
            return None
        if isinstance(v, str):
            try:
                return json.loads(v)
            except (ValueError, TypeError):
                return None
        return v  # ja e list (vindo de payload, nao do banco)
```

4. Em `ParticipanteOut` (L322), adicionar antes de `class Config`:
```python
    faixas: list["FaixaOut"] = []
    itens: list["ItemOut"] = []
```

5. Em `EventoResumo` (L299), adicionar antes do fim da classe:
```python
    itens_por_tipo: list["ResumoItemTipo"] = []
```
</action>
<verify><automated>cd backend && python -c "
from schemas import EventoOut, EventoCreate, EventoUpdate, ParticipanteOut, EventoResumo
e = EventoOut(id=1, tipo='galeto', titulo='X', descricao=None, data_inicio=None, data_fim=None, local=None, custo_estimado=0, custo_real=0, status='planejado', created_at=None, tipos_item='[\"cru\",\"assado\"]')
assert e.tipos_item == ['cru','assado'], e.tipos_item
e2 = EventoOut(id=1, tipo='x', titulo='X', descricao=None, data_inicio=None, data_fim=None, local=None, custo_estimado=0, custo_real=0, status='planejado', created_at=None, tipos_item=None)
assert e2.tipos_item is None
print('ok')
"</automated></verify>
<done>`EventoOut.tipos_item` aceita str JSON e devolve list; aceita None; `ParticipanteOut` e `EventoResumo` aceitam os novos campos com defaults vazios.</done>
</task>

<task id="3" type="auto">
<files>backend/tests/__init__.py, backend/tests/conftest.py</files>
<action>
Criar pasta de testes e fixtures reutilizaveis. Criar `backend/tests/__init__.py` vazio.

Criar `backend/tests/conftest.py`:
```python
import os
import sys
import pytest
from fastapi.testclient import TestClient
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from sqlalchemy.pool import StaticPool

# garantir import dos modulos do backend (rodando de qualquer cwd)
BACKEND_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
if BACKEND_DIR not in sys.path:
    sys.path.insert(0, BACKEND_DIR)

from database import Base, get_db
from auth import get_current_user
from models import Usuario, Evento, EventoParticipante


@pytest.fixture()
def db_engine():
    # sqlite in-memory unico compartilhado entre conexoes (StaticPool)
    engine = create_engine(
        "sqlite://",
        connect_args={"check_same_thread": False},
        poolclass=StaticPool,
    )
    Base.metadata.create_all(bind=engine)
    yield engine
    Base.metadata.drop_all(bind=engine)
    engine.dispose()


@pytest.fixture()
def TestingSession(db_engine):
    return sessionmaker(autocommit=False, autoflush=False, bind=db_engine)


@pytest.fixture()
def client(db_engine, TestingSession):
    # import tardio: app monta routers no import
    from main import app

    def _override_get_db():
        db = TestingSession()
        try:
            yield db
        finally:
            db.close()

    def _override_user():
        return Usuario(id=1, username="admin", password_hash="x", nome="Admin", role="admin")

    app.dependency_overrides[get_db] = _override_get_db
    app.dependency_overrides[get_current_user] = _override_user
    with TestClient(app) as c:
        yield c
    app.dependency_overrides.clear()


@pytest.fixture()
def evento_galeto(TestingSession):
    """Cria um evento galeto com tipos_item cru/assado e devolve seu id."""
    import json
    db = TestingSession()
    try:
        e = Evento(
            tipo="galeto", titulo="Galeto Teste", status="planejado",
            valor_cartao=20.0, custo_cartao=0.0,
            tipos_item=json.dumps(["cru", "assado"]),
        )
        db.add(e)
        db.commit()
        db.refresh(e)
        return e.id
    finally:
        db.close()


@pytest.fixture()
def participante(TestingSession, evento_galeto):
    """Participante no evento_galeto sem faixas, com 0 vendidos. Devolve (evento_id, participante_id)."""
    db = TestingSession()
    try:
        p = EventoParticipante(
            evento_id=evento_galeto, jogador_id=None, nome_avulso="Joao",
            status="pendente", pago=0, valor=0, qtd_cartoes_recebidos=0,
            qtd_vendidos=0, qtd_devolvidos=0, qtd_pagou_custo=0,
        )
        db.add(p)
        db.commit()
        db.refresh(p)
        return evento_galeto, p.id
    finally:
        db.close()
```

IMPORTANTE: `main.py` chama `start_scheduler()` e `seed_defaults` no startup. Como o teste usa `TestClient(app)` com `with`, os eventos de startup rodam. Se o startup falhar por DB (o app usa `engine` real, nao o de teste, no startup), verifique: a) se `main.py` faz `create_all` no import (modulo), isso usa o engine de prod/sqlite local — tolera. b) Se `start_scheduler` exigir rede/credenciais e quebrar o teste, adicionar `monkeypatch`/override no conftest para desabilitar o scheduler em ambiente de teste (setar env var `TESTING=1` e guardar em main, OU usar `app.router.on_startup` vazio). Investigue `services/scheduler.py:start_scheduler` antes; se for inofensivo (apenas agenda jobs em memoria sem chamada externa no startup), deixe rodar. Caso contrario, no conftest faca antes de criar o client:
```python
import main
monkeypatch.setattr(main, "start_scheduler", lambda: None, raising=False)
```
(adicione `monkeypatch` como argumento da fixture `client`).
</action>
<verify><automated>cd backend && python -c "import tests.conftest; print('conftest importa ok')"</automated></verify>
<done>conftest.py importa sem erro; fixtures `client`, `evento_galeto`, `participante`, `TestingSession`, `db_engine` definidas.</done>
</task>

<task id="4" type="auto">
<files>backend/requirements-dev.txt, backend/tests/test_schemas_smoke.py</files>
<action>
Criar `backend/requirements-dev.txt`:
```
pytest==8.3.4
```
(httpx 0.28.1 ja esta em requirements.txt — TestClient depende dele.)

Criar `backend/tests/test_schemas_smoke.py` com um smoke test que valida o harness end-to-end:
```python
def test_get_evento_retorna_tipos_item_como_lista(client, evento_galeto):
    r = client.get(f"/api/eventos/{evento_galeto}")
    assert r.status_code == 200, r.text
    body = r.json()
    assert body["tipos_item"] == ["cru", "assado"]


def test_participante_out_tem_faixas_e_itens_vazios(client, participante):
    evento_id, pid = participante
    r = client.get(f"/api/eventos/{evento_id}/participantes")
    assert r.status_code == 200, r.text
    p = next(x for x in r.json() if x["id"] == pid)
    assert p["faixas"] == []
    assert p["itens"] == []
```

Antes de marcar concluido, instalar deps de teste:
```bash
pip install -r requirements-dev.txt
```
</action>
<verify><automated>cd backend && pip install -q -r requirements-dev.txt && python -m pytest tests/test_schemas_smoke.py -x -q</automated></verify>
<done>`pytest tests/test_schemas_smoke.py` passa: GET evento retorna `tipos_item` como lista; ParticipanteOut serializa `faixas`/`itens` vazios via API real.</done>
</task>

## Criterios de Sucesso

- [ ] Os 7 schemas novos (`FaixaCreate/Update/Out`, `ItemTipo/ItensUpdate/ItemOut`, `ResumoItemTipo`) importam sem erro
- [ ] `EventoOut.tipos_item` desserializa str JSON do banco em `list[str]` (aceita None)
- [ ] `EventoCreate/Update` aceitam `tipos_item: list[str]`
- [ ] `ParticipanteOut` expoe `faixas` e `itens`; `EventoResumo` expoe `itens_por_tipo`
- [ ] Harness pytest roda: `pytest tests/test_schemas_smoke.py` verde com client autenticado e DB isolado
