---
phase: 02-frontend-portal
plan: 02-005
type: feature
autonomous: true
wave: 2
depends_on: [02-002]
requirements: [UI-02, UI-05]
must_haves:
  truths:
    - "Cada evento vira um card com arrecadado, custo e liquido (sobrou)"
    - "Liquido positivo aparece em verde, negativo em vermelho"
    - "O rotulo do custo muda conforme custo_origem: real->'custo', estimado->'custo previsto', sem_custo->'a confirmar'"
    - "Quando nao ha eventos, mostra estado vazio amigavel ('Nenhum evento ainda')"
  artifacts:
    - path: "frontend/src/components/portal/EventosBloco.tsx"
      provides: "Bloco Eventos: titulo + lista de cards por evento (arrecadou/custou/sobrou) + estado vazio"
  key_links:
    - from: "EventosBloco.tsx"
      to: "evento.custo_origem"
      via: "mapa de rotulo (real/estimado/sem_custo -> texto pt-BR)"
    - from: "EventosBloco.tsx"
      to: "evento.liquido"
      via: "cor condicional verde (>0) / vermelho (<0)"
---

# Fase 02 Plano 005: Bloco Eventos (card por evento, liquido colorido)

**Objetivo:** Implementar `EventosBloco`: um card por evento com arrecadou/custou/sobrou, líquido
colorido (verde positivo / vermelho negativo) e rótulo de custo conforme `custo_origem`. Cobre a
parte de Eventos de UI-05 e parte de UI-02. Estado vazio quando não há eventos.

## Contexto

@frontend/src/types/portal.ts - `PortalEvento { titulo, tipo, data, arrecadado, custo, custo_origem, liquido, status }` e `PortalCustoOrigem`.
@frontend/src/lib/utils.ts - `formatCurrency`, `formatDate` (formata YYYY-MM-DD -> DD/MM/AAAA), `cn`.
@frontend/src/components/ui/Card.tsx - `Card`. Reusar.
@frontend/src/components/ui/EmptyState.tsx - `EmptyState` (icon, title, description). Reusar pro vazio.
@.plano/SYSTEM-DESIGN.md - secao 3 regra de custo: real->"custo", estimado->"custo previsto", sem_custo->"a confirmar". E status planejado pode aparecer como "em breve".
@frontend/tailwind.config.ts - tokens. Eventos usam Card, txt-secondary pros labels, emerald/brand-red pro liquido.

## Dependencia de fase

Plano 002 (page scaffold) feito. `eventos[]` vem do contrato da Fase 01 (ja filtrado:
concluido + em_andamento, planejado so se arrecadou, sem cancelado, ordenado por data desc).

## Tarefas

<task id="1" type="auto">
<files>frontend/src/components/portal/EventosBloco.tsx</files>
<action>
Criar `EventosBloco`. CLIENT COMPONENT ("use client", framer-motion). Props: `{ eventos: PortalEvento[] }`.

Helper de rótulo de custo:
```
real      -> "custo"
estimado  -> "custo previsto"
sem_custo -> "a confirmar"
```

Helper de cor do líquido: `liquido > 0` -> emerald-400; `liquido < 0` -> brand-red; `== 0` -> txt-secondary.

Cada card de evento mostra:
- titulo (font-display) + um chip pequeno com `tipo` (ex: viagem/baile) e, se `status === "planejado"`,
  um chip "em breve" (amarelo/amber).
- data formatada (`formatDate(evento.data)` se nao-null, senao "sem data").
- 3 linhas: "Arrecadou {formatCurrency(arrecadado)}" / "{rotuloCusto}: {formatCurrency(custo)}" /
  "Sobrou {formatCurrency(liquido)}" com a cor condicional no líquido. Se `custo_origem === "sem_custo"`,
  o valor de custo e 0 e o rotulo "a confirmar" deixa claro.

Estado vazio: se `eventos.length === 0`, usar `EmptyState` com icone `CalendarRange` (lucide),
title "Nenhum evento ainda", description "Os resultados dos eventos aparecem aqui.".

Conteudo EXATO:

```tsx
"use client";

import { motion } from "framer-motion";
import { PartyPopper, CalendarRange } from "lucide-react";
import { Card } from "@/components/ui";
import { EmptyState } from "@/components/ui";
import { formatCurrency, formatDate } from "@/lib/utils";
import type { PortalEvento, PortalCustoOrigem } from "@/types/portal";

interface EventosBlocoProps {
  eventos: PortalEvento[];
}

const ROTULO_CUSTO: Record<PortalCustoOrigem, string> = {
  real: "Custo",
  estimado: "Custo previsto",
  sem_custo: "A confirmar",
};

function corLiquido(liquido: number): string {
  if (liquido > 0) return "text-emerald-400";
  if (liquido < 0) return "text-brand-red";
  return "text-txt-secondary";
}

export function EventosBloco({ eventos }: EventosBlocoProps) {
  return (
    <motion.section
      data-block="eventos"
      initial={{ opacity: 0, y: 16 }}
      whileInView={{ opacity: 1, y: 0 }}
      viewport={{ once: true, margin: "-40px" }}
      transition={{ duration: 0.4, ease: [0.16, 1, 0.3, 1] }}
      className="space-y-4"
    >
      <h2 className="flex items-center gap-2 font-display text-lg font-semibold text-txt-primary">
        <PartyPopper className="h-5 w-5 text-brand-red" /> Eventos
      </h2>

      {eventos.length === 0 ? (
        <EmptyState
          icon={<CalendarRange />}
          title="Nenhum evento ainda"
          description="Os resultados dos eventos aparecem aqui."
        />
      ) : (
        <div className="space-y-3">
          {eventos.map((ev, i) => (
            <Card key={`${ev.titulo}-${i}`} padding="md">
              <div className="mb-3 flex items-center justify-between gap-2">
                <div className="min-w-0">
                  <p className="truncate font-display text-base font-semibold text-txt-primary">
                    {ev.titulo}
                  </p>
                  <p className="font-body text-xs text-txt-tertiary">
                    {ev.data ? formatDate(ev.data) : "sem data"}
                  </p>
                </div>
                <div className="flex shrink-0 items-center gap-1.5">
                  <span className="rounded-full bg-surface-tertiary px-2 py-0.5 font-body text-[11px] uppercase tracking-wide text-txt-secondary">
                    {ev.tipo}
                  </span>
                  {ev.status === "planejado" && (
                    <span className="rounded-full bg-amber-500/10 px-2 py-0.5 font-body text-[11px] uppercase tracking-wide text-amber-400">
                      em breve
                    </span>
                  )}
                </div>
              </div>

              <div className="grid grid-cols-3 gap-2 text-center">
                <div>
                  <p className="font-body text-[11px] uppercase tracking-wide text-txt-tertiary">
                    Arrecadou
                  </p>
                  <p className="font-display text-sm font-semibold tabular-nums text-txt-primary">
                    {formatCurrency(ev.arrecadado)}
                  </p>
                </div>
                <div>
                  <p className="font-body text-[11px] uppercase tracking-wide text-txt-tertiary">
                    {ROTULO_CUSTO[ev.custo_origem]}
                  </p>
                  <p className="font-display text-sm font-semibold tabular-nums text-txt-secondary">
                    {formatCurrency(ev.custo)}
                  </p>
                </div>
                <div>
                  <p className="font-body text-[11px] uppercase tracking-wide text-txt-tertiary">
                    Sobrou
                  </p>
                  <p
                    className={
                      "font-display text-sm font-semibold tabular-nums " +
                      corLiquido(ev.liquido)
                    }
                  >
                    {formatCurrency(ev.liquido)}
                  </p>
                </div>
              </div>
            </Card>
          ))}
        </div>
      )}
    </motion.section>
  );
}
```

Notas:
- `ROTULO_CUSTO` cobre exatamente os 3 valores de `custo_origem` (typesafe via Record).
- amber-400/500 (escala Tailwind) so pro chip "em breve" de planejado; coerente com dark.
- `formatDate` ja trata YYYY-MM-DD (lib/utils linha 15-19).
</action>
<verify>
<automated>cd frontend && test -f "src/components/portal/EventosBloco.tsx" && grep -q "ROTULO_CUSTO" "src/components/portal/EventosBloco.tsx" && grep -q "Custo previsto" "src/components/portal/EventosBloco.tsx" && grep -q "A confirmar" "src/components/portal/EventosBloco.tsx" && grep -q "corLiquido" "src/components/portal/EventosBloco.tsx" && grep -q "Nenhum evento ainda" "src/components/portal/EventosBloco.tsx" && npx tsc --noEmit 2>&1 | grep -i "EventosBloco" && exit 1 || echo "OK EventosBloco"</automated>
</verify>
<done>`EventosBloco.tsx` renderiza card por evento com arrecadou/custou/sobrou, líquido colorido (verde/vermelho), rótulo de custo por `custo_origem` (custo/custo previsto/a confirmar), chip "em breve" pra planejado, e EmptyState quando vazio. Compila sem erro.</done>
</task>

<task id="2" type="auto">
<files>frontend/src/components/portal/index.ts</files>
<action>
Adicionar/descomentar no barrel: `export { EventosBloco } from "./EventosBloco";`.
</action>
<verify>
<automated>cd frontend && grep -q "export { EventosBloco }" "src/components/portal/index.ts" && npx tsc --noEmit 2>&1 | grep -iE "EventosBloco|portal/index" && exit 1 || echo "OK barrel eventos"</automated>
</verify>
<done>Barrel `components/portal/index.ts` exporta `EventosBloco`; `tsc --noEmit` limpo.</done>
</task>

## Criterios de Sucesso

- [ ] `EventosBloco.tsx`: card por evento com arrecadou/custou/sobrou
- [ ] Líquido positivo verde / negativo vermelho
- [ ] Rótulo de custo conforme `custo_origem` (real->Custo, estimado->Custo previsto, sem_custo->A confirmar)
- [ ] Estado vazio "Nenhum evento ainda" quando lista vazia
- [ ] Exportado no barrel; `tsc --noEmit` limpo; nenhuma lib nova
