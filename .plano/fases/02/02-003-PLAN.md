---
phase: 02-frontend-portal
plan: 02-003
type: feature
autonomous: true
wave: 2
depends_on: [02-002]
requirements: [UI-04, UI-05]
must_haves:
  truths:
    - "O Hero mostra escudo + 'Velhos Parceiros F.C.' + 'Prestacao de Contas' + saldo_atual em numero heroi"
    - "O Hero mostra o carimbo 'atualizado em DD/MM as HH:MM' (BRT) de meta.atualizado_em"
    - "O bloco Caixa mostra cards total_entrou e total_saiu e 2 badges de atraso (N mensalidades, N jogadores)"
  artifacts:
    - path: "frontend/src/components/portal/HeroCaixa.tsx"
      provides: "Hero: escudo, nome, subtitulo, saldo heroi, carimbo BRT, animacao framer-motion"
    - path: "frontend/src/components/portal/CaixaBloco.tsx"
      provides: "Cards entrou/saiu + 2 badges de atraso + slot pro grafico (FluxoChart no plano 004)"
  key_links:
    - from: "HeroCaixa.tsx"
      to: "formatAtualizadoEm + formatCurrency"
      via: "import de lib/portal e lib/utils, recebe PortalMeta e PortalCaixa por props"
    - from: "CaixaBloco.tsx"
      to: "caixa.atrasos.mensalidades / caixa.atrasos.jogadores"
      via: "badges rounded-full mostrando os COUNTs"
---

# Fase 02 Plano 003: Bloco Hero + Bloco Caixa (cards e badges)

**Objetivo:** Implementar os componentes `HeroCaixa` (escudo, nome, subtitulo, saldo herói, carimbo
BRT) e `CaixaBloco` (cards entrou/saiu + 2 badges de atraso + slot pro grafico). Cobre UI-04 (hero
+ carimbo + responsivo) e a parte de Caixa de UI-05 (cards + badges). O grafico de fluxo (UI-03) e
um plano separado (004) que se encaixa no slot deixado aqui.

## Contexto

@frontend/src/types/portal.ts - `PortalMeta`, `PortalCaixa`, `PortalCaixaAtrasos` (plano 001).
@frontend/src/lib/portal.ts - `formatAtualizadoEm(iso)` (plano 001) pro carimbo.
@frontend/src/lib/utils.ts - `formatCurrency(value)` (Intl pt-BR BRL) pros valores; `cn`.
@frontend/src/components/ui/Card.tsx - `Card` (bg-surface-card, rounded-xl, border-border-subtle, shadow-card). Reusar.
@frontend/public/icons/icon-192.svg - escudo VP.
@frontend/tailwind.config.ts - tokens: brand.red #E31E24, surface.card #14141A, surface.tertiary #1A1A1F, txt.primary #F5F5F7, txt.secondary #8E8E9A, txt.tertiary #5C5C6A, border.subtle. Fonts: font-display (oswald), font-body (dm-sans).
@frontend/src/app/(app)/eventos/page.tsx - linha 4 + 285: padrao de uso de `motion` do framer-motion ja no projeto. Reusar o mesmo estilo.

## Dependencia de fase

Plano 002 (route group + page scaffold) deve estar feito. Fase 01 alimenta os dados reais; sem ela
o componente ainda renderiza (recebe props), mas a prova com dado real depende do backend.

## Tarefas

<task id="1" type="auto">
<files>frontend/src/components/portal/HeroCaixa.tsx</files>
<action>
Criar o componente Hero. CLIENT COMPONENT ("use client" porque usa framer-motion). Props:
`{ meta: PortalMeta; caixa: PortalCaixa }`. Layout vertical centrado, mobile-first.

Estrutura:
- escudo (icon-192.svg) ~64px, centrado
- nome "Velhos Parceiros F.C." em font-display, txt-primary
- subtitulo "Prestacao de Contas" em font-body, txt-secondary, uppercase tracking
- saldo herói: `formatCurrency(caixa.saldo_atual)` GRANDE (text-4xl sm:text-5xl), font-display,
  cor: brand-red se >= 0? Nao -> saldo positivo em txt-primary/branco com leve glow; se negativo,
  vermelho. Decisao: saldo >= 0 -> `text-txt-primary`; saldo < 0 -> `text-brand-red`. Acima do saldo,
  um label pequeno "Saldo em caixa" em txt-tertiary.
- carimbo: "atualizado em " + `formatAtualizadoEm(meta.atualizado_em)` em txt-tertiary text-xs.
- animacao de entrada: envolver num `motion.div` com `initial={{ opacity: 0, y: 12 }}`,
  `animate={{ opacity: 1, y: 0 }}`, `transition={{ duration: 0.4, ease: [0.16,1,0.3,1] }}`.

Conteudo EXATO:

```tsx
"use client";

import { motion } from "framer-motion";
import { formatCurrency } from "@/lib/utils";
import { formatAtualizadoEm } from "@/lib/portal";
import type { PortalMeta, PortalCaixa } from "@/types/portal";

interface HeroCaixaProps {
  meta: PortalMeta;
  caixa: PortalCaixa;
}

export function HeroCaixa({ meta, caixa }: HeroCaixaProps) {
  const saldoNegativo = caixa.saldo_atual < 0;
  return (
    <motion.section
      data-block="hero"
      initial={{ opacity: 0, y: 12 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ duration: 0.4, ease: [0.16, 1, 0.3, 1] }}
      className="flex flex-col items-center gap-3 py-8 text-center"
    >
      {/* eslint-disable-next-line @next/next/no-img-element */}
      <img
        src="/icons/icon-192.svg"
        alt="Escudo Velhos Parceiros F.C."
        className="h-16 w-16 rounded-2xl shadow-brand"
        width={64}
        height={64}
      />
      <h1 className="font-display text-xl font-bold tracking-tight text-txt-primary">
        Velhos Parceiros F.C.
      </h1>
      <p className="font-body text-xs uppercase tracking-[0.2em] text-txt-secondary">
        Prestacao de Contas
      </p>

      <div className="mt-4 flex flex-col items-center gap-1">
        <span className="font-body text-xs uppercase tracking-wide text-txt-tertiary">
          Saldo em caixa
        </span>
        <span
          className={
            "font-display text-4xl font-bold tabular-nums sm:text-5xl " +
            (saldoNegativo ? "text-brand-red" : "text-txt-primary")
          }
        >
          {formatCurrency(caixa.saldo_atual)}
        </span>
      </div>

      <p className="mt-2 font-body text-xs text-txt-tertiary">
        atualizado em {formatAtualizadoEm(meta.atualizado_em)}
      </p>
    </motion.section>
  );
}
```
</action>
<verify>
<automated>cd frontend && test -f "src/components/portal/HeroCaixa.tsx" && grep -q "formatAtualizadoEm" "src/components/portal/HeroCaixa.tsx" && grep -q "formatCurrency" "src/components/portal/HeroCaixa.tsx" && grep -q "Prestacao de Contas" "src/components/portal/HeroCaixa.tsx" && npx tsc --noEmit 2>&1 | grep -i "HeroCaixa" && exit 1 || echo "OK HeroCaixa"</automated>
</verify>
<done>`HeroCaixa.tsx` renderiza escudo + nome + "Prestacao de Contas" + saldo via formatCurrency + carimbo via formatAtualizadoEm. Saldo negativo em brand-red. Compila sem erro.</done>
</task>

<task id="2" type="auto">
<files>frontend/src/components/portal/CaixaBloco.tsx</files>
<action>
Criar o bloco Caixa. CLIENT COMPONENT ("use client", usa framer-motion). Props: `{ caixa: PortalCaixa }`.
Conteudo:
- titulo do bloco "Caixa" com icone `Wallet` (lucide-react), font-display.
- grid de 2 cards (grid-cols-2 gap-3): card "Entrou" (total_entrou, valor em emerald-400) e card
  "Saiu" (total_saiu, valor em brand-red). Cada card usa o componente `Card` do ui, com label
  txt-secondary + valor font-display tabular-nums.
- SLOT do grafico: uma `<div data-slot="fluxo">` com o componente `FluxoChart` (plano 004). NESTE
  plano, deixar o import comentado e um placeholder `<div>` no slot (o plano 004 troca). Manter o
  `data-slot="fluxo"` pra prova visual.
- 2 badges de atraso (rounded-full): "{N} mensalidades em atraso" e "{N} jogadores em atraso",
  usando `caixa.atrasos.mensalidades` e `caixa.atrasos.jogadores`. Cor: fundo brand-red-muted, texto
  brand-red, com icone `AlertTriangle`. Se ambos os COUNTs forem 0, mostrar 1 badge verde
  "Sem atrasos no mes" (emerald). Usar `flex flex-wrap gap-2`.

Conteudo EXATO:

```tsx
"use client";

import { motion } from "framer-motion";
import { Wallet, AlertTriangle, CheckCircle2 } from "lucide-react";
import { Card } from "@/components/ui";
import { formatCurrency } from "@/lib/utils";
import type { PortalCaixa } from "@/types/portal";
// Plano 004: import { FluxoChart } from "@/components/portal/FluxoChart";

interface CaixaBlocoProps {
  caixa: PortalCaixa;
}

export function CaixaBloco({ caixa }: CaixaBlocoProps) {
  const semAtrasos =
    caixa.atrasos.mensalidades === 0 && caixa.atrasos.jogadores === 0;

  return (
    <motion.section
      data-block="caixa"
      initial={{ opacity: 0, y: 16 }}
      whileInView={{ opacity: 1, y: 0 }}
      viewport={{ once: true, margin: "-40px" }}
      transition={{ duration: 0.4, ease: [0.16, 1, 0.3, 1] }}
      className="space-y-4"
    >
      <h2 className="flex items-center gap-2 font-display text-lg font-semibold text-txt-primary">
        <Wallet className="h-5 w-5 text-brand-red" /> Caixa
      </h2>

      <div className="grid grid-cols-2 gap-3">
        <Card padding="md">
          <p className="font-body text-xs uppercase tracking-wide text-txt-secondary">
            Entrou
          </p>
          <p className="mt-1 font-display text-lg font-bold tabular-nums text-emerald-400">
            {formatCurrency(caixa.total_entrou)}
          </p>
        </Card>
        <Card padding="md">
          <p className="font-body text-xs uppercase tracking-wide text-txt-secondary">
            Saiu
          </p>
          <p className="mt-1 font-display text-lg font-bold tabular-nums text-brand-red">
            {formatCurrency(caixa.total_saiu)}
          </p>
        </Card>
      </div>

      {/* Plano 004 substitui por <FluxoChart fluxo={caixa.fluxo_12m} /> */}
      <div data-slot="fluxo" className="text-txt-tertiary">
        [grafico fluxo 12m]
      </div>

      <div className="flex flex-wrap gap-2">
        {semAtrasos ? (
          <span className="inline-flex items-center gap-1.5 rounded-full bg-emerald-500/10 px-3 py-1 font-body text-xs font-medium text-emerald-400">
            <CheckCircle2 className="h-3.5 w-3.5" /> Sem atrasos no mes
          </span>
        ) : (
          <>
            <span className="inline-flex items-center gap-1.5 rounded-full bg-brand-red-muted px-3 py-1 font-body text-xs font-medium text-brand-red">
              <AlertTriangle className="h-3.5 w-3.5" />
              {caixa.atrasos.mensalidades} mensalidades em atraso
            </span>
            <span className="inline-flex items-center gap-1.5 rounded-full bg-brand-red-muted px-3 py-1 font-body text-xs font-medium text-brand-red">
              <AlertTriangle className="h-3.5 w-3.5" />
              {caixa.atrasos.jogadores} jogadores em atraso
            </span>
          </>
        )}
      </div>
    </motion.section>
  );
}
```

Notas:
- `bg-brand-red-muted` e o token `rgba(227,30,36,0.12)` ja existente.
- O slot `data-slot="fluxo"` e onde o plano 004 injeta o grafico. NAO remover o atributo.
- emerald-400/500 sao a escala Tailwind padrao permitida pelo DESIGN-TOKENS pro "positivo".
</action>
<verify>
<automated>cd frontend && test -f "src/components/portal/CaixaBloco.tsx" && grep -q "atrasos.mensalidades" "src/components/portal/CaixaBloco.tsx" && grep -q "atrasos.jogadores" "src/components/portal/CaixaBloco.tsx" && grep -q "data-slot=\"fluxo\"" "src/components/portal/CaixaBloco.tsx" && grep -q "rounded-full" "src/components/portal/CaixaBloco.tsx" && npx tsc --noEmit 2>&1 | grep -i "CaixaBloco" && exit 1 || echo "OK CaixaBloco"</automated>
</verify>
<done>`CaixaBloco.tsx` mostra cards entrou (emerald) / saiu (brand-red), 2 badges rounded-full com os COUNTs de atraso (ou badge "sem atrasos" se 0), e mantem o slot `data-slot="fluxo"`. Compila sem erro.</done>
</task>

<task id="3" type="auto">
<files>frontend/src/components/portal/index.ts</files>
<action>
Criar o barrel de exports do portal pra os planos seguintes e a page importarem limpo.
Conteudo (sera completado pelos planos 004/005/006 conforme criam os componentes):

```typescript
export { HeroCaixa } from "./HeroCaixa";
export { CaixaBloco } from "./CaixaBloco";
// export { FluxoChart } from "./FluxoChart";   // plano 004
// export { EventosBloco } from "./EventosBloco"; // plano 005
// export { JogosBloco } from "./JogosBloco";     // plano 006
```
</action>
<verify>
<automated>cd frontend && test -f "src/components/portal/index.ts" && grep -q "HeroCaixa" "src/components/portal/index.ts" && grep -q "CaixaBloco" "src/components/portal/index.ts" && echo "OK barrel portal"</automated>
</verify>
<done>`components/portal/index.ts` exporta `HeroCaixa` e `CaixaBloco`.</done>
</task>

## Criterios de Sucesso

- [ ] `HeroCaixa.tsx`: escudo + nome + "Prestacao de Contas" + saldo herói (formatCurrency) + carimbo BRT (formatAtualizadoEm)
- [ ] Saldo negativo aparece em brand-red; positivo em txt-primary
- [ ] `CaixaBloco.tsx`: cards entrou (emerald) / saiu (brand-red) + 2 badges com os COUNTs de atraso
- [ ] Slot `data-slot="fluxo"` preservado pro plano 004 injetar o grafico
- [ ] Barrel `components/portal/index.ts` exporta os 2 componentes; `tsc --noEmit` limpo
