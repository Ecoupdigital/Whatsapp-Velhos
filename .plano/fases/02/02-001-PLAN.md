---
phase: 02-frontend-portal
plan: 02-001
type: feature
autonomous: true
wave: 0
depends_on: []
requirements: [UI-02, UI-04, DEPLOY-02]
must_haves:
  truths:
    - "Os tipos TypeScript Portal* espelham campo a campo o contrato de GET /api/portal"
    - "Existe uma funcao fetchPortal() que busca /api/portal SEM Authorization e SEM redirecionar pro /login"
    - "Existe um helper que formata meta.atualizado_em (ISO) em 'DD/MM HH:MM' no fuso BRT"
  artifacts:
    - path: "frontend/src/types/portal.ts"
      provides: "Tipos PortalResponse e aninhados, espelho do contrato"
    - path: "frontend/src/lib/portal.ts"
      provides: "fetchPortal() (fetch direto, sem interceptor de 401) + formatAtualizadoEm()"
  key_links:
    - from: "lib/portal.ts fetchPortal()"
      to: "/api/portal (rewrite same-origin)"
      via: "fetch('/api/portal') nativo, cache: 'no-store', sem header Authorization"
---

# Fase 02 Plano 001: Tipos Portal + fetch publico + formatacao BRT

**Objetivo:** Criar a fundacao de dados do portal publico. Tipos TypeScript que espelham
exatamente o contrato de `GET /api/portal` (fonte da verdade: `.plano/SYSTEM-DESIGN.md` secao 3),
uma funcao de fetch que NAO usa o `apiFetch` existente (porque ele redireciona pro `/login` em
qualquer 401, o que quebraria a pagina publica), e um helper de formatacao de data em BRT.
Todos os componentes dos planos seguintes consomem estes tipos e este fetch.

## Contexto

@frontend/src/lib/api.ts - apiFetch existente. ATENCAO: linha 26-32, em status 401 ele faz `window.location.href = "/login"`. A pagina publica NAO pode passar por isso. Por isso este plano cria um fetch dedicado.
@frontend/src/lib/utils.ts - ja tem `formatCurrency`, `cn`. NAO reescrever; o portal reusa `formatCurrency`.
@frontend/src/types/index.ts - tipos existentes (RankingEntry, FluxoMensal). NAO editar; criar arquivo novo `portal.ts` pra isolar o contrato publico.
@.plano/SYSTEM-DESIGN.md - secao 3 (contrato campo a campo) e secao 4 (mapeamento). FONTE DA VERDADE.

## Dependencia de fase

Fase 01 (backend) deve estar implementada e o endpoint `GET /api/portal` respondendo o shape
da secao 3 do SYSTEM-DESIGN. Estes tipos sao o espelho desse shape. Se a Fase 01 ainda nao
rodou, os tipos ficam corretos pelo contrato, mas a prova de runtime (planos seguintes) exige
o backend de pe.

## Tarefas

<task id="1" type="auto">
<files>frontend/src/types/portal.ts</files>
<action>
Criar o arquivo novo com os tipos que espelham campo a campo o contrato de GET /api/portal
(SYSTEM-DESIGN secao 3). Conteudo EXATO (copiar, ajustando apenas se o contrato divergir):

```typescript
// === Portal de Transparencia (contrato publico GET /api/portal) ===
// Espelho de .plano/SYSTEM-DESIGN.md secao 3. Nao misturar com types/index.ts.

export interface PortalMeta {
  time_nome: string;
  atualizado_em: string; // ISO 8601 UTC; formatar pra BRT no front
}

export interface PortalFluxoMes {
  mes: string;       // "2026-06"
  entradas: number;
  saidas: number;
}

export interface PortalCaixaAtrasos {
  mensalidades: number; // COUNT
  jogadores: number;    // COUNT DISTINCT
}

export interface PortalCaixa {
  saldo_atual: number;
  total_entrou: number;
  total_saiu: number;
  entrou_mes: number;
  saiu_mes: number;
  fluxo_12m: PortalFluxoMes[]; // ate 12 itens, ordem cronologica asc
  atrasos: PortalCaixaAtrasos;
}

export type PortalCustoOrigem = "real" | "estimado" | "sem_custo";

export interface PortalEvento {
  titulo: string;
  tipo: string;
  data: string | null;        // YYYY-MM-DD ou null
  arrecadado: number;
  custo: number;
  custo_origem: PortalCustoOrigem;
  liquido: number;
  status: string;             // concluido | em_andamento | planejado
}

export interface PortalJogosResumo {
  vitorias: number;
  empates: number;
  derrotas: number;
  gols_pro: number;
  gols_contra: number;
}

export interface PortalRankingEntry {
  nome: string;
  quantidade: number;
}

export interface PortalResultado {
  data: string;
  adversario: string;
  placar: string; // "2x1"
}

export interface PortalProximoJogo {
  data: string;
  horario: string | null;
  local: string | null;
  adversario: string;
}

export interface PortalJogos {
  resumo: PortalJogosResumo;
  artilharia: PortalRankingEntry[];
  assistencias: PortalRankingEntry[];
  destaques: PortalRankingEntry[];
  ultimos_resultados: PortalResultado[];
  proximos_jogos: PortalProximoJogo[];
}

export interface PortalResponse {
  meta: PortalMeta;
  caixa: PortalCaixa;
  eventos: PortalEvento[];
  jogos: PortalJogos;
}
```

Regra: os nomes e tipos devem bater 1:1 com o JSON da secao 3 do SYSTEM-DESIGN. Nao inventar
campos novos, nao renomear. `data` de evento e nullable; demais conforme o contrato.
</action>
<verify>
<automated>cd frontend && npx tsc --noEmit 2>&1 | grep -i "portal.ts" || echo "OK: portal.ts compila sem erro"</automated>
</verify>
<done>Arquivo `frontend/src/types/portal.ts` existe, exporta `PortalResponse` e todos os subtipos, e compila sob `tsc --noEmit` sem erro proprio.</done>
</task>

<task id="2" type="auto">
<files>frontend/src/lib/portal.ts</files>
<action>
Criar arquivo novo com a funcao de fetch publica e o formatador de data BRT.

Importante: NAO usar `apiFetch` de `lib/api.ts` porque ele redireciona pro `/login` no status 401
(quebraria a pagina publica). Usar `fetch` nativo direto, sem header Authorization. O rewrite de
`next.config.mjs` (`/api/:path*` -> BACKEND_URL) garante same-origin, entao basta o path relativo.

Conteudo EXATO:

```typescript
import type { PortalResponse } from "@/types/portal";

/**
 * Busca o pacote agregado do portal publico.
 * Fetch direto (sem o interceptor de 401 de apiFetch, que redireciona pro /login).
 * Sem header Authorization: /api/portal e publico.
 * cache: "no-store" -> sempre dado fresco (carimbo "atualizado em" em tempo real).
 */
export async function fetchPortal(): Promise<PortalResponse> {
  const res = await fetch("/api/portal", {
    method: "GET",
    headers: { Accept: "application/json" },
    cache: "no-store",
  });
  if (!res.ok) {
    throw new Error(`Falha ao carregar o portal (HTTP ${res.status})`);
  }
  return res.json() as Promise<PortalResponse>;
}

/**
 * Formata o ISO de meta.atualizado_em em "DD/MM as HH:MM" no fuso de Brasilia.
 * Usa Intl com timeZone America/Sao_Paulo (estavel, sem depender do TZ do navegador).
 */
export function formatAtualizadoEm(iso: string): string {
  const d = new Date(iso);
  if (isNaN(d.getTime())) return "";
  const fmt = new Intl.DateTimeFormat("pt-BR", {
    timeZone: "America/Sao_Paulo",
    day: "2-digit",
    month: "2-digit",
    hour: "2-digit",
    minute: "2-digit",
    hour12: false,
  });
  // Intl pt-BR ja entrega "04/06 14:30"; normaliza pro formato pedido.
  const parts = fmt.formatToParts(d);
  const get = (t: string) => parts.find((p) => p.type === t)?.value ?? "";
  return `${get("day")}/${get("month")} as ${get("hour")}:${get("minute")}`;
}
```

Observacao: usar `Intl` com `timeZone: "America/Sao_Paulo"` (em vez de `date-fns`) garante BRT
correto independente do fuso do servidor/navegador. `date-fns` esta disponivel se preferir, mas
formatacao com fuso fixo e mais simples com Intl. Nao instalar nada.
</action>
<verify>
<automated>cd frontend && npx tsc --noEmit 2>&1 | grep -i "lib/portal.ts" || echo "OK: lib/portal.ts compila sem erro"</automated>
</verify>
<done>`frontend/src/lib/portal.ts` exporta `fetchPortal` (fetch nativo sem Authorization, sem redirect de 401) e `formatAtualizadoEm` (BRT America/Sao_Paulo). Compila sem erro proprio.</done>
</task>

<task id="3" type="auto">
<files>frontend/src/lib/portal.ts</files>
<action>
Sanity check de runtime do formatador (sem precisar do backend). Confirmar que
`formatAtualizadoEm("2026-06-04T17:30:00Z")` retorna no formato "DD/MM as HH:MM" e que o offset
BRT (UTC-3) foi aplicado (17:30 UTC vira 14:30 BRT). Rodar via node inline com ts:

Criar uma checagem temporaria nao e necessario alterar arquivos: rodar o comando do verify.
Se o output nao bater, ajustar a implementacao do formatador na tarefa 2.
</action>
<verify>
<automated>cd frontend && node -e "const f=new Intl.DateTimeFormat('pt-BR',{timeZone:'America/Sao_Paulo',day:'2-digit',month:'2-digit',hour:'2-digit',minute:'2-digit',hour12:false});const p=f.formatToParts(new Date('2026-06-04T17:30:00Z'));const g=t=>p.find(x=>x.type===t).value;const out=g('day')+'/'+g('month')+' as '+g('hour')+':'+g('minute');console.log(out);if(out!=='04/06 as 14:30'){process.exit(1)}"</automated>
</verify>
<done>O comando imprime `04/06 as 14:30` (17:30 UTC convertido pra 14:30 BRT) e sai com codigo 0.</done>
</task>

## Criterios de Sucesso

- [ ] `frontend/src/types/portal.ts` exporta `PortalResponse` espelhando o contrato (SYSTEM-DESIGN secao 3)
- [ ] `frontend/src/lib/portal.ts` exporta `fetchPortal()` que usa `fetch` nativo sem Authorization e sem redirect de 401
- [ ] `frontend/src/lib/portal.ts` exporta `formatAtualizadoEm()` que entrega "DD/MM as HH:MM" em BRT
- [ ] `npx tsc --noEmit` nao acusa erro nos dois arquivos novos
- [ ] Nenhuma lib nova instalada (so `fetch`, `Intl`, tipos)
