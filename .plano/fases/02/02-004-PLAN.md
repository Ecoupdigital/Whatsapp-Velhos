---
phase: 02-frontend-portal
plan: 02-004
type: feature
autonomous: true
wave: 2
depends_on: [02-003]
requirements: [UI-03]
must_haves:
  truths:
    - "O grafico de fluxo 12 meses renderiza com recharts (entradas vs saidas) a partir de caixa.fluxo_12m"
    - "O grafico e responsivo (ResponsiveContainer) e usa as cores do tema (entradas brand-red/emerald, saidas cinza)"
    - "Quando fluxo_12m esta vazio, mostra estado vazio amigavel em vez de grafico quebrado"
  artifacts:
    - path: "frontend/src/components/portal/FluxoChart.tsx"
      provides: "Grafico recharts de barras entradas vs saidas dos ultimos 12 meses, responsivo, com tooltip pt-BR"
  key_links:
    - from: "FluxoChart.tsx"
      to: "caixa.fluxo_12m (PortalFluxoMes[])"
      via: "props fluxo: PortalFluxoMes[], mapeado pra chartData {mes label curto, Entradas, Saidas}"
    - from: "CaixaBloco.tsx slot data-slot=fluxo"
      to: "FluxoChart"
      via: "import e render do FluxoChart no slot (substitui o placeholder do plano 003)"
---

# Fase 02 Plano 004: Grafico de fluxo 12 meses (recharts)

**Objetivo:** Implementar `FluxoChart` com recharts (ja instalado, ^3.8) reproduzindo o padrao do
dashboard interno (BarChart entradas vs saidas, ResponsiveContainer, tooltip custom pt-BR) a partir
de `caixa.fluxo_12m`, e encaixar no slot `data-slot="fluxo"` do `CaixaBloco`. Cobre UI-03.

## Contexto

@frontend/src/app/(app)/dashboard/page.tsx - linhas 18-25 (imports recharts) e 401-444 (BarChart de fluxo). REFERENCIA exata do padrao: `ResponsiveContainer`, `BarChart`, `CartesianGrid strokeDasharray="3 3"`, `XAxis dataKey="mes"`, `YAxis` com `tickFormatter` (k), `Tooltip` custom, dois `<Bar>` (Entradas fill #E31E24, Saidas fill #3A3A48). Copiar esse estilo.
@frontend/src/types/portal.ts - `PortalFluxoMes { mes: string; entradas: number; saidas: number }`.
@frontend/src/lib/utils.ts - `formatCurrency` pro tooltip.
@frontend/src/components/portal/CaixaBloco.tsx - tem o slot `data-slot="fluxo"` (plano 003) onde o grafico entra.
@frontend/src/components/portal/index.ts - barrel a atualizar com `FluxoChart`.

## Dependencia de fase

Plano 003 (CaixaBloco com o slot) deve estar feito. `caixa.fluxo_12m` vem do contrato da Fase 01.

## Tarefas

<task id="1" type="auto">
<files>frontend/src/components/portal/FluxoChart.tsx</files>
<action>
Criar `FluxoChart`. CLIENT COMPONENT ("use client", recharts exige). Props: `{ fluxo: PortalFluxoMes[] }`.
Mapear `fluxo` pra `chartData` encurtando o label do mes ("2026-06" -> "Jun" ou "06/26"; usar
mes/ano curto). Renderizar BarChart copiando o padrao do dashboard (cores e eixos identicos).
Tooltip custom mostrando mes + Entradas/Saidas em formatCurrency. Se `fluxo` vazio, retornar
EmptyState ("Sem movimentacao nos ultimos meses").

Conteudo EXATO:

```tsx
"use client";

import {
  BarChart,
  Bar,
  XAxis,
  YAxis,
  CartesianGrid,
  Tooltip,
  ResponsiveContainer,
  type TooltipProps,
} from "recharts";
import { formatCurrency } from "@/lib/utils";
import type { PortalFluxoMes } from "@/types/portal";

interface FluxoChartProps {
  fluxo: PortalFluxoMes[];
}

const MESES_CURTOS = [
  "Jan", "Fev", "Mar", "Abr", "Mai", "Jun",
  "Jul", "Ago", "Set", "Out", "Nov", "Dez",
];

function labelMes(mes: string): string {
  // "2026-06" -> "Jun"
  const parts = mes.split("-");
  const m = parseInt(parts[1] ?? "0", 10);
  return MESES_CURTOS[m - 1] ?? mes;
}

function CustomTooltip({ active, payload, label }: TooltipProps<number, string>) {
  if (!active || !payload || payload.length === 0) return null;
  return (
    <div className="rounded-lg border border-border bg-surface-elevated px-3 py-2 shadow-card">
      <p className="mb-1 font-body text-xs font-medium text-txt-secondary">{label}</p>
      {payload.map((p) => (
        <p
          key={p.name}
          className="font-body text-xs tabular-nums"
          style={{ color: p.color }}
        >
          {p.name}: {formatCurrency(Number(p.value ?? 0))}
        </p>
      ))}
    </div>
  );
}

export function FluxoChart({ fluxo }: FluxoChartProps) {
  if (!fluxo || fluxo.length === 0) {
    return (
      <div className="flex h-40 items-center justify-center rounded-xl border border-border-subtle bg-surface-card">
        <p className="font-body text-sm text-txt-tertiary">
          Sem movimentacao nos ultimos meses
        </p>
      </div>
    );
  }

  const chartData = fluxo.map((f) => ({
    mes: labelMes(f.mes),
    Entradas: f.entradas,
    Saidas: f.saidas,
  }));

  return (
    <div className="rounded-xl border border-border-subtle bg-surface-card p-3 sm:p-4">
      <p className="mb-3 font-body text-xs uppercase tracking-wide text-txt-secondary">
        Fluxo dos ultimos 12 meses
      </p>
      <div className="h-[200px] sm:h-[260px]">
        <ResponsiveContainer width="100%" height="100%">
          <BarChart data={chartData} margin={{ top: 4, right: 4, bottom: 4, left: 4 }}>
            <CartesianGrid strokeDasharray="3 3" stroke="#1F1F27" vertical={false} />
            <XAxis
              dataKey="mes"
              tick={{ fill: "#8E8E9A", fontSize: 12 }}
              axisLine={false}
              tickLine={false}
            />
            <YAxis
              tick={{ fill: "#8E8E9A", fontSize: 12 }}
              axisLine={false}
              tickLine={false}
              width={36}
              tickFormatter={(v: number) =>
                v >= 1000 ? `${(v / 1000).toFixed(0)}k` : String(v)
              }
            />
            <Tooltip
              content={<CustomTooltip />}
              cursor={{ fill: "rgba(255,255,255,0.03)" }}
            />
            <Bar dataKey="Entradas" fill="#10B981" radius={[4, 4, 0, 0]} maxBarSize={28} />
            <Bar dataKey="Saidas" fill="#E31E24" radius={[4, 4, 0, 0]} maxBarSize={28} />
          </BarChart>
        </ResponsiveContainer>
      </div>
    </div>
  );
}
```

Notas:
- Entradas em emerald (#10B981) e Saidas em brand-red (#E31E24): mesma semantica de cor do resto do
  portal (positivo verde, negativo vermelho), coerente com DESIGN-TOKENS. (No dashboard interno as
  cores eram outras; aqui priorizamos a semantica do portal.)
- `tabular-nums` e `formatCurrency` no tooltip dao leitura limpa no mobile.
- recharts 3.8: `TooltipProps<number, string>` e o tipo correto pro content custom.
</action>
<verify>
<automated>cd frontend && test -f "src/components/portal/FluxoChart.tsx" && grep -q "ResponsiveContainer" "src/components/portal/FluxoChart.tsx" && grep -q "BarChart" "src/components/portal/FluxoChart.tsx" && grep -q "fluxo_12m\|fluxo:" "src/components/portal/FluxoChart.tsx" && npx tsc --noEmit 2>&1 | grep -i "FluxoChart" && exit 1 || echo "OK FluxoChart"</automated>
</verify>
<done>`FluxoChart.tsx` renderiza BarChart recharts (Entradas vs Saidas) responsivo a partir de `fluxo`, com tooltip pt-BR e estado vazio. Compila sem erro.</done>
</task>

<task id="2" type="auto">
<files>frontend/src/components/portal/CaixaBloco.tsx, frontend/src/components/portal/index.ts</files>
<action>
Encaixar o FluxoChart no slot do CaixaBloco e exportar no barrel.

Em `CaixaBloco.tsx`:
1. Descomentar/adicionar o import: `import { FluxoChart } from "./FluxoChart";`
2. Substituir o placeholder do slot. Trocar o bloco:
   ```tsx
   <div data-slot="fluxo" className="text-txt-tertiary">
     [grafico fluxo 12m]
   </div>
   ```
   por:
   ```tsx
   <div data-slot="fluxo">
     <FluxoChart fluxo={caixa.fluxo_12m} />
   </div>
   ```
   Manter o atributo `data-slot="fluxo"` (gancho de prova visual).

Em `components/portal/index.ts`: descomentar/adicionar
`export { FluxoChart } from "./FluxoChart";`.
</action>
<verify>
<automated>cd frontend && grep -q "import { FluxoChart }" "src/components/portal/CaixaBloco.tsx" && grep -q "<FluxoChart fluxo={caixa.fluxo_12m}" "src/components/portal/CaixaBloco.tsx" && grep -q "export { FluxoChart }" "src/components/portal/index.ts" && npx tsc --noEmit 2>&1 | grep -iE "CaixaBloco|FluxoChart|portal/index" && exit 1 || echo "OK fluxo encaixado"</automated>
</verify>
<done>`CaixaBloco` importa e renderiza `<FluxoChart fluxo={caixa.fluxo_12m} />` no slot `data-slot="fluxo"`; barrel exporta `FluxoChart`; `tsc --noEmit` limpo.</done>
</task>

## Criterios de Sucesso

- [ ] `FluxoChart.tsx` usa recharts (ResponsiveContainer + BarChart) com Entradas (verde) vs Saidas (vermelho)
- [ ] Le `caixa.fluxo_12m`, encurta labels de mes, tem tooltip pt-BR com formatCurrency
- [ ] Estado vazio amigavel quando `fluxo` vem vazio
- [ ] Encaixado no slot `data-slot="fluxo"` do CaixaBloco; exportado no barrel
- [ ] `tsc --noEmit` limpo; nenhuma lib nova (recharts ja estava no package.json)
