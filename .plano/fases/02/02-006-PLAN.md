---
phase: 02-frontend-portal
plan: 02-006
type: feature
autonomous: true
wave: 2
depends_on: [02-002]
requirements: [UI-02]
must_haves:
  truths:
    - "O bloco Em campo mostra cards V/E/D e gols_pro/gols_contra do resumo"
    - "Mostra rankings de artilharia, assistencias e destaques (top N) com icone lucide e nome + quantidade"
    - "Mostra ultimos_resultados (placares) e proximos_jogos (agenda: data/horario/local/adversario)"
    - "Cada sub-secao tem estado vazio amigavel quando a lista correspondente vem vazia"
  artifacts:
    - path: "frontend/src/components/portal/JogosBloco.tsx"
      provides: "Bloco esportivo completo: resumo V/E/D + gols, 3 rankings, ultimos resultados, proximos jogos"
  key_links:
    - from: "JogosBloco.tsx"
      to: "jogos.resumo / artilharia / assistencias / destaques / ultimos_resultados / proximos_jogos"
      via: "props jogos: PortalJogos, render por sub-secao"
---

# Fase 02 Plano 006: Bloco Em campo (jogos)

**Objetivo:** Implementar `JogosBloco`: cards de aproveitamento (V/E/D) + gols pró/contra, rankings
(artilharia/assistências/destaques top N com ícone), últimos resultados (placares) e próximos jogos
(agenda). Cobre a parte esportiva de UI-02. Cada sub-secão com estado vazio.

## Contexto

@frontend/src/types/portal.ts - `PortalJogos`, `PortalJogosResumo`, `PortalRankingEntry`, `PortalResultado`, `PortalProximoJogo`.
@frontend/src/lib/utils.ts - `formatDate` (YYYY-MM-DD -> DD/MM/AAAA), `cn`.
@frontend/src/components/ui/Card.tsx - `Card`. Reusar.
@frontend/src/components/ui/EmptyState.tsx - `EmptyState`.
@frontend/tailwind.config.ts - tokens. V em emerald, E em txt-secondary, D em brand-red. Rankings com ícones lucide.

## Dependencia de fase

Plano 002 (page scaffold). `jogos` vem do contrato da Fase 01 (resumo de `/jogos/estatisticas`,
rankings de `/jogos/rankings`, resultados/proximos derivados de `realizado`).

## Tarefas

<task id="1" type="auto">
<files>frontend/src/components/portal/JogosBloco.tsx</files>
<action>
Criar `JogosBloco`. CLIENT COMPONENT ("use client", framer-motion). Props: `{ jogos: PortalJogos }`.

Sub-componente interno `RankingList({ titulo, icon, entries })` reutilizado pros 3 rankings:
recebe `entries: PortalRankingEntry[]`, mostra top 5 (`entries.slice(0,5)`) numa lista ordenada
(1., 2., ...) com nome + quantidade; se vazio, linha "sem registros".

Estrutura do bloco:
1. titulo "Em campo" com icone `Trophy` (lucide).
2. linha de 5 cards compactos (grid-cols-5 gap-2): Vitorias (emerald), Empates (txt-secondary),
   Derrotas (brand-red), Gols pro, Gols contra. Numero grande font-display + label minusculo.
3. 3 rankings via `RankingList`: artilharia (icone `Goal`), assistencias (icone `Handshake`),
   destaques (icone `Star`). Grid responsivo: 1 coluna mobile, 3 em sm+.
4. "Ultimos resultados": lista de `ultimos_resultados` mostrando "vs {adversario}" + placar + data;
   placar em chip. Se vazio, EmptyState curto.
5. "Proximos jogos": lista de `proximos_jogos` com data (formatDate), horario (se nao-null), local
   (se nao-null), adversario. Se vazio, EmptyState curto.

Conteudo EXATO:

```tsx
"use client";

import { motion } from "framer-motion";
import {
  Trophy,
  Goal,
  Handshake,
  Star,
  CalendarClock,
  History,
} from "lucide-react";
import { Card } from "@/components/ui";
import { EmptyState } from "@/components/ui";
import { formatDate } from "@/lib/utils";
import type {
  PortalJogos,
  PortalRankingEntry,
} from "@/types/portal";

interface JogosBlocoProps {
  jogos: PortalJogos;
}

function RankingList({
  titulo,
  icon,
  entries,
}: {
  titulo: string;
  icon: React.ReactNode;
  entries: PortalRankingEntry[];
}) {
  const top = entries.slice(0, 5);
  return (
    <Card padding="md">
      <p className="mb-2 flex items-center gap-1.5 font-display text-sm font-semibold text-txt-primary">
        <span className="text-brand-red">{icon}</span> {titulo}
      </p>
      {top.length === 0 ? (
        <p className="font-body text-xs text-txt-tertiary">sem registros</p>
      ) : (
        <ol className="space-y-1.5">
          {top.map((e, i) => (
            <li
              key={`${e.nome}-${i}`}
              className="flex items-center justify-between gap-2 font-body text-sm"
            >
              <span className="truncate text-txt-secondary">
                {i + 1}. {e.nome}
              </span>
              <span className="shrink-0 font-display font-semibold tabular-nums text-txt-primary">
                {e.quantidade}
              </span>
            </li>
          ))}
        </ol>
      )}
    </Card>
  );
}

function StatCard({
  valor,
  label,
  cor,
}: {
  valor: number;
  label: string;
  cor: string;
}) {
  return (
    <Card padding="sm" className="text-center">
      <p className={"font-display text-xl font-bold tabular-nums " + cor}>{valor}</p>
      <p className="font-body text-[10px] uppercase tracking-wide text-txt-tertiary">
        {label}
      </p>
    </Card>
  );
}

export function JogosBloco({ jogos }: JogosBlocoProps) {
  const { resumo, artilharia, assistencias, destaques, ultimos_resultados, proximos_jogos } =
    jogos;

  return (
    <motion.section
      data-block="jogos"
      initial={{ opacity: 0, y: 16 }}
      whileInView={{ opacity: 1, y: 0 }}
      viewport={{ once: true, margin: "-40px" }}
      transition={{ duration: 0.4, ease: [0.16, 1, 0.3, 1] }}
      className="space-y-4"
    >
      <h2 className="flex items-center gap-2 font-display text-lg font-semibold text-txt-primary">
        <Trophy className="h-5 w-5 text-brand-red" /> Em campo
      </h2>

      <div className="grid grid-cols-5 gap-2">
        <StatCard valor={resumo.vitorias} label="V" cor="text-emerald-400" />
        <StatCard valor={resumo.empates} label="E" cor="text-txt-secondary" />
        <StatCard valor={resumo.derrotas} label="D" cor="text-brand-red" />
        <StatCard valor={resumo.gols_pro} label="Gols pro" cor="text-txt-primary" />
        <StatCard valor={resumo.gols_contra} label="Gols contra" cor="text-txt-primary" />
      </div>

      <div className="grid grid-cols-1 gap-3 sm:grid-cols-3">
        <RankingList titulo="Artilharia" icon={<Goal className="h-4 w-4" />} entries={artilharia} />
        <RankingList titulo="Assistencias" icon={<Handshake className="h-4 w-4" />} entries={assistencias} />
        <RankingList titulo="Destaques" icon={<Star className="h-4 w-4" />} entries={destaques} />
      </div>

      <div>
        <p className="mb-2 flex items-center gap-1.5 font-display text-sm font-semibold text-txt-primary">
          <History className="h-4 w-4 text-brand-red" /> Ultimos resultados
        </p>
        {ultimos_resultados.length === 0 ? (
          <EmptyState title="Sem jogos registrados" />
        ) : (
          <div className="space-y-2">
            {ultimos_resultados.map((r, i) => (
              <Card key={`${r.adversario}-${i}`} padding="sm">
                <div className="flex items-center justify-between gap-2">
                  <div className="min-w-0">
                    <p className="truncate font-body text-sm text-txt-secondary">
                      vs {r.adversario}
                    </p>
                    <p className="font-body text-xs text-txt-tertiary">{formatDate(r.data)}</p>
                  </div>
                  <span className="shrink-0 rounded-md bg-surface-tertiary px-2.5 py-1 font-display text-sm font-bold tabular-nums text-txt-primary">
                    {r.placar}
                  </span>
                </div>
              </Card>
            ))}
          </div>
        )}
      </div>

      <div>
        <p className="mb-2 flex items-center gap-1.5 font-display text-sm font-semibold text-txt-primary">
          <CalendarClock className="h-4 w-4 text-brand-red" /> Proximos jogos
        </p>
        {proximos_jogos.length === 0 ? (
          <EmptyState title="Sem jogos agendados" />
        ) : (
          <div className="space-y-2">
            {proximos_jogos.map((j, i) => (
              <Card key={`${j.adversario}-${i}`} padding="sm">
                <p className="font-body text-sm text-txt-primary">vs {j.adversario}</p>
                <p className="font-body text-xs text-txt-tertiary">
                  {formatDate(j.data)}
                  {j.horario ? ` as ${j.horario}` : ""}
                  {j.local ? ` - ${j.local}` : ""}
                </p>
              </Card>
            ))}
          </div>
        )}
      </div>
    </motion.section>
  );
}
```

Notas:
- `grid-cols-5` no resumo cabe no mobile (5 numeros curtos); se ficar apertado, os labels sao bem
  curtos (V/E/D/Gols pro/Gols contra). Mantido mobile-first.
- EmptyState aqui usa so `title` (description opcional no componente).
- Icones lucide escolhidos por sub-secao (Goal/Handshake/Star/History/CalendarClock).
</action>
<verify>
<automated>cd frontend && test -f "src/components/portal/JogosBloco.tsx" && grep -q "resumo.vitorias" "src/components/portal/JogosBloco.tsx" && grep -q "RankingList" "src/components/portal/JogosBloco.tsx" && grep -q "ultimos_resultados" "src/components/portal/JogosBloco.tsx" && grep -q "proximos_jogos" "src/components/portal/JogosBloco.tsx" && npx tsc --noEmit 2>&1 | grep -i "JogosBloco" && exit 1 || echo "OK JogosBloco"</automated>
</verify>
<done>`JogosBloco.tsx` mostra V/E/D + gols, 3 rankings (top 5) com icone, ultimos resultados (placar) e proximos jogos (agenda), cada um com estado vazio. Compila sem erro.</done>
</task>

<task id="2" type="auto">
<files>frontend/src/components/portal/index.ts</files>
<action>
Adicionar/descomentar no barrel: `export { JogosBloco } from "./JogosBloco";`.
</action>
<verify>
<automated>cd frontend && grep -q "export { JogosBloco }" "src/components/portal/index.ts" && npx tsc --noEmit 2>&1 | grep -iE "JogosBloco|portal/index" && exit 1 || echo "OK barrel jogos"</automated>
</verify>
<done>Barrel exporta `JogosBloco`; `tsc --noEmit` limpo.</done>
</task>

## Criterios de Sucesso

- [ ] `JogosBloco.tsx`: cards V/E/D + gols pró/contra do `resumo`
- [ ] 3 rankings (artilharia/assistências/destaques) top 5 com ícone lucide, nome + quantidade
- [ ] Últimos resultados com placar; próximos jogos com data/horario/local/adversario
- [ ] Cada sub-secão tem estado vazio amigável
- [ ] Exportado no barrel; `tsc --noEmit` limpo; nenhuma lib nova
