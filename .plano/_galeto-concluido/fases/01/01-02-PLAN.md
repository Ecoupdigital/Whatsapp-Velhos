---
phase: 01-schema-migracao-backfill
plan: 01-02
type: feature
autonomous: true
wave: 1
depends_on: [01-01]
requirements: [MIG-01, MIG-02, MIG-03, MIG-05]
must_haves:
  truths:
    - "run_additive_migrations(engine) adiciona a coluna eventos.tipos_item apenas se ela ainda nao existe, via inspect, sem IF NOT EXISTS"
    - "O backfill cria no maximo 1 faixa por participante que ainda nao tem faixa: numerada se tem numero_inicio+fim, sem_numero se so tem qtd_cartoes_recebidos>0"
    - "run_additive_migrations e chamada no boot do main.py logo apos create_all"
    - "Nenhuma coluna ou tabela pre-existente e dropada ou renomeada"
  artifacts:
    - path: "backend/migrations.py"
      provides: "run_additive_migrations(engine), _add_column_tipos_item(engine), _backfill_faixas(engine)"
    - path: "backend/main.py"
      provides: "Chamada run_additive_migrations(engine) apos Base.metadata.create_all"
  key_links:
    - from: "main.py boot"
      to: "migrations.run_additive_migrations"
      via: "import + chamada logo apos create_all(bind=engine)"
    - from: "_backfill_faixas"
      to: "evento_cartao_faixa"
      via: "INSERT 1 faixa por participante sem faixa (guard por COUNT==0 = idempotencia)"
---

# Fase 01 Plano 01-02: migrations.py (ADD COLUMN idempotente + backfill) e wiring no boot

**Objetivo:** Criar `backend/migrations.py` com `run_additive_migrations(engine)`, chamada no boot do `main.py` logo apos `Base.metadata.create_all`. A funcao (a) adiciona `eventos.tipos_item` apenas se ainda nao existe (via `inspect`, portavel Postgres+SQLite, sem `IF NOT EXISTS`), e (b) faz backfill idempotente das faixas a partir dos dados legados. FASE SENSIVEL: roda em producao no boot. DEVE ser estritamente aditiva (MIG-05) e rodavel N vezes sem duplicar nem falhar (TEST-06, provado no plano 01-03).

## Contexto

@backend/migrations.py - arquivo NOVO a criar.
@backend/main.py - L18-19 tem o comentario `# Create tables ... no ALTER TABLE patches needed` e `Base.metadata.create_all(bind=engine)`. Este plano introduz a primeira chamada de migracao no boot do projeto. O comentario L18 deve ser atualizado.
@backend/database.py - de onde vem `engine`, `Base`, `sessionmaker`/`SessionLocal`.
@backend/models.py - apos plano 01-01 contem `EventoParticipante`, `EventoCartaoFaixa`. Imports de modelos no migrations.py sao ABSOLUTOS (`from models import ...`), igual ao resto do backend (ver `main.py` L7-8).
@.plano/SYSTEM-DESIGN.md - secao 6 tem o codigo de referencia (6.2 ALTER idempotente, 6.3 backfill).

Convencoes criticas:
- Imports do backend sao absolutos (rodando com cwd em `backend/`): `from models import ...`, `from database import engine, Base`. NAO usar `from backend.models import`.
- `sem_numero` e Integer 0/1.
- `ALTER TABLE eventos ADD COLUMN tipos_item TEXT` e valido em Postgres E SQLite. NAO usar `IF NOT EXISTS` (SQLite nao suporta uniformemente).
- Usar `sqlalchemy.inspect(engine)` para checar existencia de coluna antes do ALTER -> idempotencia do ADD COLUMN nos dois bancos.

## Tarefas

<task id="1" type="auto">
<files>backend/migrations.py</files>
<action>
Criar `backend/migrations.py` com a funcao de ADD COLUMN idempotente. Codigo:

```python
"""Migracoes aditivas idempotentes rodadas no boot (main.py), apos create_all.

Regras (MIG-03/MIG-05):
- Estritamente aditivo: nunca DROP nem rename de tabela/coluna existente.
- Portavel Postgres + SQLite: ADD COLUMN ... TEXT, sem IF NOT EXISTS, JSON como Text.
- Idempotente: checa via inspect() antes de alterar; backfill so toca participante sem faixa.
"""
import logging

from sqlalchemy import inspect, text
from sqlalchemy.orm import sessionmaker

log = logging.getLogger(__name__)


def _add_column_tipos_item(engine):
    """ADD COLUMN eventos.tipos_item TEXT, somente se ainda nao existe.

    Portavel Postgres+SQLite (sem IF NOT EXISTS). Idempotente via inspect().
    """
    insp = inspect(engine)
    cols = [c["name"] for c in insp.get_columns("eventos")]
    if "tipos_item" not in cols:
        with engine.begin() as conn:
            conn.execute(text("ALTER TABLE eventos ADD COLUMN tipos_item TEXT"))
        log.info("migrations: coluna eventos.tipos_item adicionada")
    else:
        log.info("migrations: eventos.tipos_item ja existe, pulando ADD COLUMN")


def run_additive_migrations(engine):
    """Ponto de entrada chamado no boot apos Base.metadata.create_all.

    Ordem: (1) ADD COLUMN tipos_item, (2) backfill de faixas.
    Seguro rodar N vezes (idempotente).
    """
    _add_column_tipos_item(engine)
    _backfill_faixas(engine)
```

Notas: `with engine.begin()` abre transacao (commit automatico no sucesso, rollback no erro). A checagem `if "tipos_item" not in cols` torna o ADD idempotente em qualquer dialeto. `_backfill_faixas` e definida na tarefa 2 (mesmo arquivo). NAO referenciar JSONB. NAO dropar nada.
</action>
<verify>
<automated>cd backend && python3 -c "
import migrations
from sqlalchemy import create_engine, inspect, Table, Column, Integer, Text, MetaData
e = create_engine('sqlite:///:memory:')
md = MetaData()
Table('eventos', md, Column('id', Integer, primary_key=True), Column('titulo', Text))
md.create_all(e)
ins0 = inspect(e); assert 'tipos_item' not in [c['name'] for c in ins0.get_columns('eventos')]
migrations._add_column_tipos_item(e)
ins1 = inspect(e); assert 'tipos_item' in [c['name'] for c in ins1.get_columns('eventos')], 'ADD COLUMN falhou'
migrations._add_column_tipos_item(e)
ins2 = inspect(e); cnt = sum(1 for c in ins2.get_columns('eventos') if c['name']=='tipos_item'); assert cnt == 1, cnt
print('OK MIG-01/MIG-03 ADD COLUMN idempotente')
"</automated>
</verify>
<done>`migrations.py` existe com `run_additive_migrations` e `_add_column_tipos_item`. ADD COLUMN roda quando a coluna falta, pula quando ja existe, e rodar 2x nao duplica nem falha. Sem `IF NOT EXISTS`.</done>
</task>

<task id="2" type="auto">
<files>backend/migrations.py</files>
<action>
Adicionar a funcao `_backfill_faixas(engine)` em `backend/migrations.py` (acima de `run_additive_migrations`, para o nome resolver na chamada). Codigo EXATO:

```python
def _backfill_faixas(engine):
    """Cria 1 faixa por participante legado que ainda NAO tem faixa.

    Idempotencia (TEST-06): so processa participante com COUNT(faixas)==0.
    Regras (MIG-02/MIG-04):
      - tem numero_inicio E numero_fim -> 1 faixa numerada, qtd = fim-ini+1
      - senao, qtd_cartoes_recebidos > 0 -> 1 faixa sem_numero=1, qtd = recebidos
      - senao -> nenhuma faixa
    Garante sum(faixas.quantidade) == qtd_cartoes_recebidos legado por participante.
    """
    from models import EventoParticipante, EventoCartaoFaixa

    Session = sessionmaker(bind=engine)
    db = Session()
    criadas = 0
    try:
        parts = db.query(EventoParticipante).all()
        for p in parts:
            ja_tem = (
                db.query(EventoCartaoFaixa)
                .filter(EventoCartaoFaixa.evento_participante_id == p.id)
                .count()
            )
            if ja_tem:
                continue
            if p.numero_inicio is not None and p.numero_fim is not None:
                qtd = p.numero_fim - p.numero_inicio + 1
                db.add(EventoCartaoFaixa(
                    evento_participante_id=p.id,
                    numero_inicio=p.numero_inicio,
                    numero_fim=p.numero_fim,
                    quantidade=qtd,
                    sem_numero=0,
                ))
                criadas += 1
            elif (p.qtd_cartoes_recebidos or 0) > 0:
                db.add(EventoCartaoFaixa(
                    evento_participante_id=p.id,
                    numero_inicio=None,
                    numero_fim=None,
                    quantidade=p.qtd_cartoes_recebidos,
                    sem_numero=1,
                ))
                criadas += 1
        db.commit()
        log.info("migrations: backfill criou %d faixa(s)", criadas)
    finally:
        db.close()
```

Notas criticas:
- O import `from models import EventoParticipante, EventoCartaoFaixa` fica DENTRO da funcao para evitar ciclo de import.
- Guard `if ja_tem: continue` = idempotencia. Participante que ja tem faixa nunca recebe faixa duplicada.
- Edge case: participante numerado onde `fim-ini+1` difere de `qtd_cartoes_recebidos`. A faixa numerada usa qtd=fim-ini+1 (fonte de verdade). A prova de contagem (MIG-04) que compara soma de faixas com recebidos legado e validada no plano 01-03 (la, qualquer inconsistencia legada aparece). NAO tentar reconciliar aqui.
- NAO gerar numero fake para lote sem_numero (decisao do briefing).
</action>
<verify>
<automated>cd backend && python3 -c "
import migrations, models
from sqlalchemy import create_engine
from database import Base
from sqlalchemy.orm import sessionmaker
e = create_engine('sqlite:///:memory:')
Base.metadata.create_all(bind=e)
S = sessionmaker(bind=e); db = S()
ev = models.Evento(tipo='confraternizacao', titulo='Galeto'); db.add(ev); db.commit()
p1 = models.EventoParticipante(evento_id=ev.id, numero_inicio=10, numero_fim=21, qtd_cartoes_recebidos=12)
p2 = models.EventoParticipante(evento_id=ev.id, qtd_cartoes_recebidos=5)
p3 = models.EventoParticipante(evento_id=ev.id, qtd_cartoes_recebidos=0)
db.add_all([p1,p2,p3]); db.commit()
migrations._backfill_faixas(e)
db2 = S()
f1 = db2.query(models.EventoCartaoFaixa).filter_by(evento_participante_id=p1.id).all()
f2 = db2.query(models.EventoCartaoFaixa).filter_by(evento_participante_id=p2.id).all()
f3 = db2.query(models.EventoCartaoFaixa).filter_by(evento_participante_id=p3.id).all()
assert len(f1)==1 and f1[0].sem_numero==0 and f1[0].quantidade==12 and f1[0].numero_inicio==10
assert len(f2)==1 and f2[0].sem_numero==1 and f2[0].quantidade==5 and f2[0].numero_inicio is None
assert len(f3)==0
print('OK MIG-02 backfill: numerada / sem_numero / vazio')
"</automated>
</verify>
<done>`_backfill_faixas` cria 1 faixa numerada (qtd=fim-ini+1, sem_numero=0) para quem tem numero, 1 faixa sem_numero (qtd=recebidos) para quem so tem recebidos>0, e nada para quem tem 0. Numeros corretos.</done>
</task>

<task id="3" type="auto">
<files>backend/main.py</files>
<action>
Conectar a migracao no boot. Em `backend/main.py`:

1. Adicionar import perto dos imports de modulos locais (apos L9 `from auth import hash_password`):
```python
from migrations import run_additive_migrations
```

2. Substituir o bloco L18-19:
```python
# Create tables (Postgres handles schema natively, no ALTER TABLE patches needed)
Base.metadata.create_all(bind=engine)
```
por:
```python
# Cria tabelas novas (create_all NAO adiciona colunas em tabela existente)
Base.metadata.create_all(bind=engine)
# Migracoes aditivas idempotentes: ADD COLUMN tipos_item + backfill de faixas (rodam no boot)
run_additive_migrations(engine)
```

Notas: a chamada DEVE vir logo apos `create_all` (as 2 tabelas novas precisam existir antes do backfill inserir faixas, e a tabela `eventos` precisa existir antes do ADD COLUMN). NAO mover para dentro de `on_startup` - manter no escopo de modulo junto do create_all (consistente com o padrao atual). Nao tocar em mais nada de main.py.
</action>
<verify>
<automated>cd backend && python3 -c "
import ast
src = open('main.py').read()
tree = ast.parse(src)
imports = [n for n in ast.walk(tree) if isinstance(n, ast.ImportFrom) and n.module=='migrations']
assert imports and any(a.name=='run_additive_migrations' for a in imports[0].names), 'falta import run_additive_migrations'
lines = src.splitlines()
ci = next(i for i,l in enumerate(lines) if 'create_all' in l)
ri = next(i for i,l in enumerate(lines) if 'run_additive_migrations(engine)' in l)
assert ri > ci, 'run_additive_migrations deve vir apos create_all'
print('OK MIG-01 wiring no boot')
"</automated>
</verify>
<done>`main.py` importa `run_additive_migrations` e o chama em escopo de modulo logo apos `Base.metadata.create_all(bind=engine)`. Comentario legado L18 atualizado.</done>
</task>

<task id="4" type="auto">
<files>backend/migrations.py</files>
<action>
Teste de fumaca end-to-end do boot real em SQLite limpo: importar `main` deve disparar `create_all` + `run_additive_migrations` sem erro, e o app deve ficar com as 2 tabelas + coluna. Esta tarefa nao escreve codigo de producao novo; valida a integracao das tarefas 1-3 simulando o boot com um `DATABASE_PATH` temporario (NAO toca o `velhos.db` real). Se falhar, corrigir tarefas anteriores.
</action>
<verify>
<automated>cd backend && rm -f /tmp/velhos_boot_test.db && DATABASE_PATH=/tmp/velhos_boot_test.db python3 -c "
import main  # dispara create_all + run_additive_migrations no import
from sqlalchemy import create_engine, inspect
e = create_engine('sqlite:////tmp/velhos_boot_test.db')
insp = inspect(e); tabs = set(insp.get_table_names())
assert 'evento_cartao_faixa' in tabs and 'evento_participante_item' in tabs, tabs
assert 'tipos_item' in [c['name'] for c in insp.get_columns('eventos')]
from migrations import run_additive_migrations
run_additive_migrations(e)
print('OK boot e2e + 2a execucao sem erro')
"; rm -f /tmp/velhos_boot_test.db</automated>
</verify>
<done>Importar `main` com DATABASE_PATH temporario sobe o app: cria as 2 tabelas novas, adiciona `tipos_item`, e rodar a migracao uma 2a vez no mesmo banco nao falha. Confirma o wiring real do boot.</done>
</task>

## Criterios de Sucesso

- [ ] `backend/migrations.py` existe com `run_additive_migrations`, `_add_column_tipos_item`, `_backfill_faixas`
- [ ] ADD COLUMN `eventos.tipos_item` so roda se a coluna falta (via inspect), sem `IF NOT EXISTS`, portavel Postgres+SQLite
- [ ] Backfill cria 1 faixa numerada ou 1 sem_numero por participante sem faixa; guard COUNT==0 garante idempotencia
- [ ] `main.py` chama `run_additive_migrations(engine)` logo apos `create_all`
- [ ] Boot e2e em SQLite limpo cria tabelas + coluna; rodar migracao 2x nao falha
- [ ] Nenhum DROP/rename (MIG-05): codigo so contem ADD COLUMN e INSERT
