---
phase: 02-api
plan: 02-05
type: feature
autonomous: true
wave: 2
depends_on: [02-01, 02-03]
requirements: [API-06]
must_haves:
  truths:
    - "GET /eventos/{id}/resumo retorna itens_por_tipo agregado por tipo do evento"
    - "Cada entrada tem tipo, total_vendido e total_pedido somados sobre todos os participantes"
    - "cartoes_emitidos soma qtd_cartoes_recebidos (ja derivado das faixas)"
  artifacts:
    - path: "backend/routers/eventos.py"
      provides: "resumo_evento agrega itens_por_tipo via group_by tipo"
    - path: "backend/tests/test_resumo.py"
      provides: "Cobertura API-06"
  key_links:
    - from: "resumo_evento"
      to: "EventoParticipanteItem agregado"
      via: "func.sum group_by tipo join participantes do evento"
---

# Fase 02 Plano 05: Resumo com itens_por_tipo

**Objetivo:** Estender `GET /eventos/{id}/resumo` para incluir `itens_por_tipo` (relacao consolidada cru x assado), agregando `qtd_vendido` e `qtd_pedido` por tipo sobre todos os participantes do evento. Cobre API-06.

## Contexto

@backend/routers/eventos.py — `resumo_evento` (L395-432); ja monta `EventoResumo`
@backend/models.py — `EventoParticipanteItem`, `EventoParticipante`
@backend/schemas.py — `EventoResumo.itens_por_tipo` e `ResumoItemTipo` (02-01)
@backend/tests/conftest.py — fixtures

> Depende de 02-01 (schema `ResumoItemTipo`/campo `itens_por_tipo`) e 02-03 (endpoint PUT itens, usado nos testes para popular dados). Confirme via grep que `EventoParticipanteItem` esta importado no router (02-03 ja adicionou); se nao, adicionar ao `from models import ...`.

## Tarefas

<task id="1" type="auto">
<files>backend/routers/eventos.py</files>
<action>
Em `resumo_evento` (L395-432):

1. Garantir imports: `EventoParticipanteItem` no `from models import ...`; `ResumoItemTipo` no `from schemas import (...)`.

2. Antes do `return EventoResumo(...)` (L418), computar a agregacao por tipo:
```python
    rows = (
        db.query(
            EventoParticipanteItem.tipo,
            func.coalesce(func.sum(EventoParticipanteItem.qtd_vendido), 0),
            func.coalesce(func.sum(EventoParticipanteItem.qtd_pedido), 0),
        )
        .join(EventoParticipante, EventoParticipante.id == EventoParticipanteItem.evento_participante_id)
        .filter(EventoParticipante.evento_id == evento_id)
        .group_by(EventoParticipanteItem.tipo)
        .all()
    )
    itens_por_tipo = [
        ResumoItemTipo(tipo=t, total_vendido=int(v or 0), total_pedido=int(ped or 0))
        for (t, v, ped) in rows
    ]
```

3. Passar `itens_por_tipo=itens_por_tipo` no construtor de `EventoResumo(...)`.

`cartoes_emitidos` ja soma `qtd_cartoes_recebidos` (L412) — agora esse valor reflete as faixas (derivado nos planos anteriores), sem mudanca de formula.
</action>
<verify><automated>cd backend && python -m pytest tests/test_schemas_smoke.py -x -q && python -c "from routers.eventos import resumo_evento, ResumoItemTipo, EventoParticipanteItem; print('ok')"</automated></verify>
<done>`resumo_evento` agrega `itens_por_tipo` por `group_by(tipo)`; imports presentes; smoke test verde.</done>
</task>

<task id="2" type="auto">
<files>backend/tests/test_resumo.py</files>
<action>
Cobrir API-06. Criar `backend/tests/test_resumo.py`. Setup: 2 participantes no evento galeto, ambos com vendidos setados e split via PUT itens; o resumo deve consolidar.

```python
from models import EventoParticipante


def _novo_participante(TestingSession, evento_id, nome, vendidos):
    db = TestingSession()
    p = EventoParticipante(
        evento_id=evento_id, jogador_id=None, nome_avulso=nome,
        status="pendente", pago=0, valor=0,
        qtd_cartoes_recebidos=0, qtd_vendidos=vendidos,
        qtd_devolvidos=0, qtd_pagou_custo=0,
    )
    db.add(p)
    db.commit()
    pid = p.id
    db.close()
    return pid


def test_resumo_itens_por_tipo_consolida(client, evento_galeto, TestingSession):
    eid = evento_galeto
    p1 = _novo_participante(TestingSession, eid, "A", 10)
    p2 = _novo_participante(TestingSession, eid, "B", 8)

    # p1: cru 6 (ped 2) + assado 4 (ped 1)
    client.put(f"/api/eventos/{eid}/participantes/{p1}/itens", json={"itens": [
        {"tipo": "cru", "qtd_vendido": 6, "qtd_pedido": 2},
        {"tipo": "assado", "qtd_vendido": 4, "qtd_pedido": 1}]})
    # p2: cru 3 (ped 0) + assado 5 (ped 3)
    client.put(f"/api/eventos/{eid}/participantes/{p2}/itens", json={"itens": [
        {"tipo": "cru", "qtd_vendido": 3, "qtd_pedido": 0},
        {"tipo": "assado", "qtd_vendido": 5, "qtd_pedido": 3}]})

    r = client.get(f"/api/eventos/{eid}/resumo")
    assert r.status_code == 200, r.text
    por_tipo = {x["tipo"]: x for x in r.json()["itens_por_tipo"]}
    assert por_tipo["cru"]["total_vendido"] == 9   # 6+3
    assert por_tipo["cru"]["total_pedido"] == 2    # 2+0
    assert por_tipo["assado"]["total_vendido"] == 9  # 4+5
    assert por_tipo["assado"]["total_pedido"] == 4   # 1+3


def test_resumo_sem_itens_lista_vazia(client, evento_galeto):
    r = client.get(f"/api/eventos/{evento_galeto}/resumo")
    assert r.status_code == 200, r.text
    assert r.json()["itens_por_tipo"] == []
```
</action>
<verify><automated>cd backend && python -m pytest tests/test_resumo.py -x -q</automated></verify>
<done>`pytest tests/test_resumo.py` verde: `itens_por_tipo` consolida vendido/pedido por tipo entre participantes; evento sem itens retorna lista vazia.</done>
</task>

<task id="3" type="auto">
<files>backend/routers/eventos.py</files>
<action>
Verificacao de regressao final da Fase 2: rodar a suite inteira garantindo que todos os planos coexistem sem conflito. Nenhuma alteracao de codigo aqui alem de eventuais correcoes se a suite acusar quebra de integracao (ex: import duplicado, helper redefinido). Se houver quebra, corrigir mantendo a semantica dos planos 02-01..02-05.
</action>
<verify><automated>cd backend && python -m pytest tests/ -q</automated></verify>
<done>Suite completa `pytest tests/` verde: smoke, faixas, itens, evento/popular, resumo todos passando juntos.</done>
</task>

## Criterios de Sucesso

- [ ] `GET /resumo` retorna `itens_por_tipo` com `{tipo, total_vendido, total_pedido}` consolidado
- [ ] Agregacao soma corretamente entre multiplos participantes
- [ ] Evento sem itens retorna `itens_por_tipo: []`
- [ ] `cartoes_emitidos` reflete recebidos derivado das faixas
- [ ] Suite completa `pytest tests/` verde
