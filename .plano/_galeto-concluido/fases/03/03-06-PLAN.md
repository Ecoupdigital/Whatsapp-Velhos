---
phase: 03-frontend
plan: 03-06
type: feature
autonomous: true
wave: 2
depends_on: [03-04]
requirements: [UI-01, UI-03, TEST-05]
must_haves:
  truths:
    - "A lista de cards de participante foi substituida pelo grid editavel na tela do evento"
    - "Editar uma celula salva em blur/Enter e o Valor recalcula sem abrir modal"
    - "Erro 400 do backend reverte a celula e mostra toast com a mensagem do backend"
    - "Pagar, remover, estornar e historico continuam funcionando"
    - "npm run build passa sem imports orfaos"
  artifacts:
    - path: "frontend/src/app/(app)/eventos/[id]/page.tsx"
      provides: "Grid integrado na Participants Section, reaproveitando handlers e modais existentes"
  key_links:
    - from: "page.tsx Participants Section"
      to: "ParticipantesGrid"
      via: "props commitCartaoCampo/commitItemCampo + renderExpanded (historico)"
---

# Fase 03 Plano 06: Integrar grid na pagina (substituir lista) + build + prova

**Objetivo:** Trocar a renderizacao em lista de cards pelo `ParticipantesGrid` (criado no 03-04) dentro da Participants Section, preservando pagar/remover/estornar/historico e todos os modais. Garantir build limpo e capturar a prova visual de UI-01, UI-03 e TEST-05. Cuidado central: nao quebrar o fluxo atual da tela durante a migracao.

## Contexto

@frontend/src/app/(app)/eventos/[id]/page.tsx — "Participants Section" (~L840-1026): hoje renderiza `filtered.map` de cards no ramo `else` de `filtered.length === 0`. Handlers/estado ja existentes: `openPayModal` (~L338), `handleRemoverParticipante` (~L327), `handleEstornar` (~L380), `expandedParticipanteId`/`setExpandedParticipanteId` (~L208), `pagamentos`, `contaNome` (~L534), `pStatusDerivado` (~L117), `nomeParticipante` (~L123), `formatCurrency`. Handlers de commit `commitCartaoCampo`/`commitItemCampo`/`refetchParticipante` e import de `ItemTipo` vieram do plano 03-04.
@frontend/src/components/eventos/ParticipantesGrid.tsx — componente do grid (plano 03-04), com prop `renderExpanded`.

Convencoes: UI kit, sem nova identidade. Toast PT-BR sem em-dash. Padrao do projeto: build trata import nao usado como erro (commit ddf3358).

## Tarefas

<task id="1" type="auto">
<files>frontend/src/app/(app)/eventos/[id]/page.tsx</files>
<action>
Importar o grid no topo do arquivo (junto aos imports de componentes):

```typescript
import { ParticipantesGrid } from "@/components/eventos/ParticipantesGrid";
```
</action>
<verify><automated>grep -nE "import \{ ParticipantesGrid \} from \"@/components/eventos/ParticipantesGrid\"" /home/projects/Whatsapp-Velhos/frontend/src/app/(app)/eventos/[id]/page.tsx</automated></verify>
<done>ParticipantesGrid importado no page.tsx.</done>
</task>

<task id="2" type="auto">
<files>frontend/src/app/(app)/eventos/[id]/page.tsx</files>
<action>
Substituir o bloco de lista de cards pelo grid. Na Participants Section, dentro do `<Card padding="lg">`, no ramo `else` do `filtered.length === 0` (o `<div className="space-y-2">` com `AnimatePresence` + `filtered.map` de cards, ~L870-1023), SUBSTITUIR TODO esse bloco por:

```tsx
<ParticipantesGrid
  participantes={filtered}
  evento={evento}
  expandedId={expandedParticipanteId}
  onToggleExpand={(id) => setExpandedParticipanteId(expandedParticipanteId === id ? null : id)}
  onPay={openPayModal}
  onRemove={handleRemoverParticipante}
  commitCartaoCampo={commitCartaoCampo}
  commitItemCampo={commitItemCampo}
  nomeParticipante={nomeParticipante}
  statusDerivado={pStatusDerivado}
  renderExpanded={(p) => {
    const hist = pagamentos.filter((pg) => pg.evento_participante_id === p.id);
    if (hist.length === 0) {
      return <p className="text-xs text-txt-tertiary font-body py-1">Sem pagamentos registrados</p>;
    }
    return (
      <div className="space-y-1">
        <p className="text-xs text-txt-tertiary uppercase tracking-wider font-body mb-1">Historico</p>
        {hist.map((pg) => (
          <div key={pg.id} className="flex items-center gap-3 text-xs py-1 px-2 rounded hover:bg-surface-card">
            <span className="font-mono text-txt-secondary">{pg.data}</span>
            <span className="font-mono text-emerald-400">{formatCurrency(pg.valor)}</span>
            {pg.forma_pagto && <span className="text-txt-tertiary uppercase">{pg.forma_pagto}</span>}
            {contaNome(pg.conta_id) && <span className="text-txt-secondary">{contaNome(pg.conta_id)}</span>}
            <div className="flex-1" />
            <button onClick={() => handleEstornar(pg.id)} className="text-txt-tertiary hover:text-red-400" title="Estornar">
              <RotateCcw size={12} />
            </button>
          </div>
        ))}
      </div>
    );
  }}
/>
```

NAO remover os modais (Add, Avulso, Pagamento, Cartoes, Edit) nem seus handlers neste plano. O modal "Cartoes" deixa de ser aberto pelo grid mas permanece definido (fallback inofensivo); a aposentadoria dele acontece no 03-05 (apos as faixas substituirem por completo). Apenas o JSX da lista de cards e removido aqui.
</action>
<verify><automated>grep -nE "<ParticipantesGrid|commitCartaoCampo=\{commitCartaoCampo\}|renderExpanded=\{" /home/projects/Whatsapp-Velhos/frontend/src/app/(app)/eventos/[id]/page.tsx</automated></verify>
<done>Lista de cards substituida pelo grid; pagar/remover/estornar/historico preservados; modais intactos.</done>
</task>

<task id="3" type="auto">
<files>frontend/src/app/(app)/eventos/[id]/page.tsx</files>
<action>
Build limpo. Rodar a partir de `frontend/`:

```bash
cd /home/projects/Whatsapp-Velhos/frontend && npx tsc --noEmit && npm run build 2>&1 | tail -30
```

Corrigir: (a) imports lucide agora orfaos por causa da remocao da lista de cards (ex: `Check`, `Pencil`, `Users` etc. SE nao usados em mais nenhum lugar) - remover apenas os que o build apontar como nao usados; `motion`/`AnimatePresence` continuam usados nos cards de stats no topo, manter; (b) qualquer erro de tipo nos props do grid. O `npm run build` DEVE passar (projeto trata import nao usado como erro de build). Validar que a tela ainda monta (sem erro de runtime no console ao abrir o evento).
</action>
<verify><automated>cd /home/projects/Whatsapp-Velhos/frontend && npx tsc --noEmit 2>&1 | grep -E "error TS" | head -5 || echo "tsc OK"</automated></verify>
<done>`npx tsc --noEmit` e `npm run build` passam; sem imports orfaos.</done>
</task>

<task id="4" type="checkpoint:human-verify">
<files>frontend/src/app/(app)/eventos/[id]/page.tsx</files>
<action>
Verificacao visual (UI-01, UI-03, TEST-05). Backend Fase 2 rodando, evento Galeto com tipos_item=["cru","assado"] e participantes. Subir o frontend, abrir `/eventos/[id]`. Capturar e validar:
1. Grid com 1 linha por participante: Participante, Receb. (read-only), Vend./Devol./Custo (editaveis), cru v / cru p / assado v / assado p (editaveis e dinamicas), Valor (read-only), Status, Acoes.
2. Clicar numa celula Vend., digitar valor valido, Enter -> salva sem modal e a coluna Valor recalcula (TEST-05 feliz).
3. Editar split (cru v) de modo que NAO feche com Vend. -> backend 400, toast com a mensagem do backend e a celula reverte ao valor anterior (TEST-05 erro).
4. Confirmar que Pagar, remover (X), expandir (historico) e os modais ainda funcionam.
5. Abrir um evento SEM tipos_item: as colunas de split NAO aparecem (UI-03).

Anexar capturas: grid normal, save inline recalculando Valor, toast de erro com revert, e evento sem split (sem colunas de tipo).
</action>
<verify><automated>FALTANDO — prova visual: capturas do grid editavel, recalc inline, toast+revert no 400 e evento sem split sem colunas de tipo.</automated></verify>
<done>Capturas comprovam grid inline (colunas dinamicas), autosave/recalc sem modal, revert+toast no 400 e ausencia de colunas de tipo quando o evento nao tem tipos.</done>
</task>

## Criterios de Sucesso

- [ ] Lista de cards substituida pelo grid
- [ ] Vend./Devol./Custo editam inline e recalculam Valor sem modal
- [ ] Colunas de split aparecem so com `evento.tipos_item` e salvam via PUT itens
- [ ] Erro 400 reverte celula + toast com mensagem do backend
- [ ] Pagar/remover/estornar/historico/modais preservados
- [ ] `npx tsc --noEmit` e `npm run build` passam
- [ ] Capturas visuais anexadas (UI-01, UI-03, TEST-05)
